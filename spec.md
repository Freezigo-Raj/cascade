# PROJECT: Cascade — Part A (Intake)
Last updated: 4 August 2026  |  Session #: 33

## WHAT IT IS

A personal execution engine. Part A is its intake: one text box that captures a typed line, classifies it, finds it again later, and orders what has been captured. Nothing reminds, nothing organises, nothing syncs. Those are Parts B, C and D, and none of them can be tuned until Part A has produced real captures to tune against.

The user is Vishal, running two businesses, capturing commitments that arrive from calls, messages and his own head faster than he can process them. Measured on a real 82-line backlog: 45 unique tasks, 37 duplicate lines, 6 compound lines, roughly a quarter with the verb at the end of the sentence.

## STAGE

Current stage: 5
Gates passed: **Gate 1**, **Gate 2**, **Gate 3** and **Gate 4**, each on an older file than the one it names. All four signatures are stale and all four are waiting on a hand. See VERSIONS.
Stage 4 is closed and signed. Stage 5 is open: the rules go in one at a time and the same command is read the other way round, so a case that still fails names a rule not yet written. `gate4.mjs` reads the stage out of the line above and inverts its own verdict on it. Every case in the key agrees with the engine. Section I taps a type, a significance and a bound task, which no case did before, and section B and H state what the card says. `gate4.mjs` reads Stage 5 and passes: 142 run, 142 green, 0 on invariants alone, 0 errored. Nine rules are written and every obligation the contract records is now met except the ranking, which is Stage 6's. Gate 5 is the hand that says Stage 5 is finished.

Status: the contract is written in the protocol's three groups, with Name, Type, Required, Unit, Range, Example and From per item. 13 inputs, 13 working values, the 44-field `Task`, the 4-field `UndoEntry`, 40 rendered outputs, 14 cross-field invariants, and config at 37 objects populated. `tsc --strict` compiles clean and `gate2.py` passes. The Stage 3 shell is built and Gate 3 is signed: the panel drew correctly by hand, and deleting `card_badge` from the placeholder produced a red box naming `resolve.js` and the missing field rather than a blank screen. `resolve()` now returns three keys, `task`, `list` and `capture`, and the contract names that shape. The answer key holds 144 key cases.

D-1: **Typing the thought is the whole of the work.**

## CONTRACT

File: `schema/contract.md` — **written.**
Machine version: `schema/types.ts` — written, compiles clean under `--strict`. Checked item-for-item against `contract.md` by `gate2.py`, across all five groups. Gate 2 is signed on the current contract and the current config.
Config: `config.ts` — see VERSIONS.
Last contract change: Session 20, 1 August 2026. Ranking tiers, `card_reason` composition, `capture_state`.

`CASCADE_PART_A_SPEC.md` spans Stages 2 to 6 and predates most of the decisions below. It is reference material, superseded by `schema/contract.md` for everything Stage 2 covers.

## HOW TO RUN

Run app:   py -m http.server 8000, then http://localhost:8000/  (both screens, what a person
           sees; the same address the phone installs). The Stage 3 harness is still at
           /shell/ and still draws every field
           (live: every keystroke
           re-resolves; no build step, shell/config.js is checked against config.ts by gate2.py)
Run tests: python3 gate2.py && python3 selftest.py && node gate4.mjs && node shell/check_render.mjs && node shell/check_loud.mjs && node shell/check_alarm.mjs && node shell/check_search.mjs && node shell/check_writes.mjs
Run key:   node gate4.mjs   (--verbose for the engine's own log lines, --section B for one section,
           --placeholder to run the current key against the frozen Stage 3 file under the Stage 4 reading)

## VERSIONS

  example      42
  contract     54
  config       a.19
  answer_key   28
  shell        47
  gate1        signed on example 35
  gate2        signed on contract 32
  gate3        signed on shell 1
  gate4        signed on answer_key 12

**Stated once.** Any version number written anywhere else in this file is a defect the gate reports.


## STATE OF PLAY

**Working and verified:**
- `gate2.py` — the gate, run as a script. Reads every count rather than trusting prose.
- `selftest.py` — 28 deliberately broken copies, each breaking one file in one way. The gate catches every one of them, and a gate that dies on a traceback is counted as a miss rather than a catch. One fixture is read by `gate4.mjs` rather than `gate2.py`, because a weekday that contradicts its date is a defect only the key runner can see.
- `shell/` — the Stage 3 shell. `check_render.mjs` reproduces example section 1 character for character from the placeholder; `check_loud.mjs` proves six deliberate breaks are loud; `check_alarm.mjs` covers the alarm records and the three tier-1 overrides, which is the first check ever to reach `rank_key`.
- `android/` — the alarm shell: six Kotlin files and the manifest additions. `shell/alarm.bridge.js` is the only thing that talks to it, and every timing number travels in the payload so nothing in Kotlin states a policy.
- `gate4.mjs` — the runner. Reads every expected value out of the answer key at run time and holds none of its own. A case that fails on a contract invariant and on nothing the key states fails the gate rather than being counted. `--placeholder` runs the current key against `shell/resolve.stage3.js` under the Stage 4 reading, so a key edit can still be checked the Stage 4 way after the engine has rules.

**Built, and only as verified as the gate above it:**
- `spec/example.md` — Stage 1 finished example. Gate 1 signed on the current file.
- `schema/contract.md`, `schema/types.ts` — Stage 2. Gate 2 signed on the current file; `tsc --strict` compiles clean.
- `config.ts` — the 37 config objects. Every vocabulary member is reachable from the lexicon.
- `shell/lemma.js` — wink-nlp and its English web model, MIT, bundled once and committed. 3.6 MB. No build step at run time: the shell serves it as a static file and the gate imports the same one. Read last in the verb chain.
- The record also carries its four handed-in values, `raw_text`, `id`, `created_at` and `config_version`, which is what makes a case able to be green at all.
- `tests/answer_key.md` — the hand-written key, Gate 4 signed on the current file. Every case a table row and every case carrying a derivation. 142 run and every one states a value; the two that do not run each name the gate that answers them.

**Built this session, unpressed:**
- `shell/mvp.html` is both screens. Screen 1 is the list; screen 2 is capture and edit, reached by `+` or by tapping a row. Neither screen imports the other: `mvp.js` hands the list a way of opening the edit screen and the edit screen a way back. `mvp.words.js` holds the chip arithmetic as pure functions and is the one part of screen 2 readable without a browser. Gate 6 is the hand that says the screens are right, and it is a sixth thing owed.

**Not started:**
- The ranking. Nine factors named in config and a direction table in the example; no code and no contract rule. Nothing orders the list.
- The list itself. `resolve()` answers for one line. `cards`, `rank_key`, `decided_by`, `group_header` and `result_row` are constants in `shell/resolve.js`, and the key names none of them.
- Storage, search, the row actions and undo. Screens beyond the two the example draws.

## THIS SESSION'S JOB

Session 131: his `UI_review_R2.pptx`, re-uploaded with new contents. THE DECK WAS READ THIS TIME.
Session 130 answered the two items he had typed in chat and never opened the file, and three of its
five items were only in the file — a stated failure, and the reason the first thing this session did
was dump every `<a:t>` in every slide rather than trusting a filename.

**ONE CHARACTER PAIR HELD A DEFECT OPEN THROUGH THREE SESSIONS.** His slide: a task with an alarm
still reading `An alarm needs an exact time`.

`resolve()` returns `due_at: ""` for a line with no date — an EMPTY STRING, not null. The alarm row
read `out.task.due_at ?? held?.due_at`, and `??` falls through on null and undefined only. So `dueAt`
came out as `""`, the fallback to the stored date never ran, and `if (hasTime && dueAt)` failed on
the empty string while `hasTime` was perfectly true. Session 126 wrote the fallback rule and session
129 fixed its timing; both were correct and both read straight past this. It is `||` now.

**THE PICKERS SIT ON THEIR OWN LINE**, his slide, and there are TWO. Inside the scrolling rung row
`Pick…` was reachable only by scrolling past every approximation of the answer. `Pick time` is one
dialog for the common case — same day, different hour — and a time already gone means TOMORROW,
because 9am chosen at 6pm can only be the morning and a push that lands overdue is a trap.
`Pick date & time` is the whole thing. Both are drawn whatever the payload carried: they invent no
date, the person is typing one.

**`Tomorrow` LEFT THE LOCK SCREEN**, his slide. Four rungs rather than five. Moving something to
another day is a decision worth being awake for; the lock screen is for delaying by hours, and the
pickers cover everything further out.

**THE APK LINK DID NOTHING, AND THERE WERE SOMETIMES TWO OF IT.** Both his, and they are unrelated
bugs in one line of code. The `download` attribute is ignored across origins, and a WebView asked to
navigate to an APK on its own host does nothing at all — no download, no error, no sound.
`target="_blank"` is the one signal Capacitor reads as "this is not for me", so the link leaves for
the system browser, which downloads it and offers to open it. And the account screen's alarm block
redraws whenever the app returns from a system settings screen, clearing the rows it owns by
removing every `[data-perm]` node first — the link carried no such mark, survived the sweep, and a
second copy was appended beside it.

**Shell build 5.** Nothing about the payload changed, so an APK on build 4 rings correctly and
simply wears the older arrangement of the same buttons.

## NEXT THREE JOBS

1. Build the Capacitor shell and run the eight alarm tests against THIS build rather than the
   standalone test app: ring, lock-screen ring, ring with the app killed, snooze re-ring, reboot
   survival, closed-app outcome drain, ringing through silent and DND, and the auto chain reaching
   its limit and writing `alarm_unanswered_at`.
2. Use it for a few days on the phone. Every defect worth having in this project was found that way.
3. The workflow migration, recorded in full under DECIDED, NOT BUILT, then Gate 6 and the five gates
   already owed.

## TYPED, AND WRONG

Lines typed into the live shell that came out wrong. Open until each is either fixed or written into the key as intended behaviour. `now` is Mon 3 Aug 10:40 for every line here.

| # | Typed | What came out | What is wrong |
|---|---|---|---|
| T1 (fixed) | `call kushan next friday` | title `call kushan next`, due Fri 7 Aug | `next` is stranded in the title, and `next friday` resolves to this Friday rather than the one after |
| T2 (fixed) | `call kushan 5.30pm` | no date at all | A time written with a dot is not read. Only `5pm` and `17:00` shapes are |
| T3 (fixed) | `call kushan 5 pm` | no date at all | A space between the number and `pm` loses the time |
| T4 (fixed) | `call kushan tomorrow friday` | due tomorrow, title `call kushan friday` | Two dates in one line and no rule for which wins. The loser stays in the title |
| T5 (fixed) | `call kushan by after friday` | title `call kushan by`, no due date | Two markers and no rule. The longest wins by accident and the loser is stranded |
| T6 (fixed) | `pay a tomorrow, b` | title `pay a b`, `date_phrase` reads `tomorrow,` | The comma is swallowed into the date span, and the item it separated disappears from the title |
| T7 (fixed) | `call kushan at 10AM` at 10:40 | due today 10:00, overdue on capture | A clock time that has already gone does not roll, while a band does. No rule says which is right |
| T8 (part fixed) | `go to office` | `other`, 5 minutes | Not a defect in a rule. The lexicon has no `go`, and a visit is not a five-minute job |
| T9 (fixed) | `call kushan tmrw` | no date | `tmrw` is in no lexicon. Abbreviations are unhandled as a family |
| T10 (fixed) | `Social alpha application deadline` | `is_hard` false | `date_firmness` is `hard` and `is_hard` reads it, per the contract. Nothing writes it, so a tier-1 ranking override is dead |
| T12 (fixed) | any line, no tap | `significance` 70 | The contract says the untouched default is 30, and the example says so twice. The placeholder carried 70 because the line it was copied from tapped High |
| T14 (fixed) | any typed line | the capture row's last line was drawn two ways | Section 2's layout is the rule: chips wrap, then three spaces, the type chip, the fill, the significance buttons and one space inside the border. Both panels in the example are redrawn that way and the `· · ·` leader is gone |
| T13 (fixed) | any line | `type_chip_tap`, `significance_tap`, `bound_task_id`, `row_action` read zero times | Four of the nine inputs were dead. Twelve frozen fields trace back to them |
| T15 (closed) | `call raj Tomorrow AM` | title `call raj AM`, due tomorrow 23:59:59 | Closed on the label. The chip is `Tomorrow morning`, which the band-on-a-named-day rule consumes whole, so nothing strands. It cost what the 4 August deviation said it would: a config bump, the four capture panels in `spec/example.md` redrawn, and `check_render` failed until they were. The gate found a second thing while it was there. The check pinned its five lines by counting from the typed line, which was exact while the chip row was two lines and wrong the moment a longer label wrapped it to three: it then compared the panel against a window one row lower and called every line a difference. It reads from the box down to the panel's closing rule now, so a chip row is free to grow. `am`/`pm` as band words was the cheaper alternative and is not needed |
| T11 (fixed) | any line with a date | `resolved_window` and `clipped_window` frozen at the morning band | The date rule computes both internally and writes neither out. They still hold the hand-copied record's values on every line |

**T10 and T11 were found by asking which fields never move,** rather than by reading any line's output. Across fifteen varied lines, 24 of the 63 returned fields changed and 39 did not; three of the 39 should have. The other 36 sort into fields no rule writes yet, fields the shell cannot move because it has no input for them, and fields that are genuinely constant.

**These are for a session of their own.** Each is a rule that does not exist rather than a rule that is wrong, except T6, which is a rule misfiring. None is caught by the key, because the key has no case for any of them, which is the point: 142 green says the rules do what they say, not that they cover what people type.

## FOUND, NOT FIXED

- Export inside the Android shell does nothing: `exportAll()` clicks a blob anchor, the Capacitor WebView has no handler for blob downloads, and no file lands anywhere. In a browser or the installed web app it lands in Downloads as `cascade-<date>.json`. Found 18 Aug (session 121) answering "export stores where?". The fix is a share sheet or a Kotlin write, both of which touch the APK, so it waits for the next Kotlin session rather than riding a screen session.

- No check ties a due date drawn in the example to a date span in the text that produced it. Session 37 found one drawn task whose text could not produce its date, by hand, after twenty-nine Gate 1 passes.

- `gate2.py` compares each drawn badge's verb against the task table but not its duration. `Payments to coolindia, sudhi, laptop` carries `duration_source: summed`, so a badge duration is not always `duration_defaults[verb]` and a naive check would fail on it. The three durations changed at re-signing were traced by hand.

- `CASCADE_PART_A_SPEC.md` is stale. It still names `date_origin`, lacks `date_phrase`, `date_hedge`, `date_marker`, `date_precision`, `date_firmness`, `date_anchor` and `is_hard`, has importance as never-asked, and carries the superseded ranking order. Found 1 Aug. Deferred because rewriting it is Stage 2 work and this session was Stage 1.
- The 20-slide engine deck is stale on the same points. Found 1 Aug. Lower priority; it is a reference artefact, not a build input.
- `date_firmness` `soft` is captured in Part A and read by nothing until Part B. Stated as a decision in the contract rather than left as a gap, and it is the one field in the record that no Part A output consumes.
- `limits.raw_text_min_chars` is 2, fitted to nothing. The rule that does the work is the letter-or-digit one; the number is a guess and should be looked at once real captures exist.
- `ListView.group_header` is required by the type and the Default list draws none. Found Session 30, still open.
- **`MVP.md` said two things about the day load and screen 1 followed one of them.** The screen-1 control table said each push button "says what the target day already holds"; nine lines later the same page says the app "says none of it" and lists the day load among the fields that never appear on any screen. Session 89 settled it and the table cell was a session-87 leftover. Corrected to "says only where it lands". The Claude Design files followed the table: every push button in all three carries a load note, and the Part A file also prints `today is full` and `A day is full at 3 hrs of committed work, summed from per-type defaults`, which leaks the per-verb duration defaults the quiet-fields rule exists to protect.
- `gate2.py` now catches a config version in companion prose, and only in prose: the same string inside a table cell is an illustrative value in an Example column and is not checked. `spec/example.md` is exempt entirely while the deviation licensing its older stamp stands.
- The audit that prompted this session was wrong about the duplicate scores, and the reading that confirmed it was wrong the same way. Both used symmetric padding; the contract states two leading spaces and one trailing, which reproduces all nine numbers exactly. Nothing was changed on the strength of it, and the lesson is that a stated definition has to be run before it is called unreproducible.
- The `list` column asserts routing on cases the engine cannot yet decide, so the green count fell from 35 to 16 the moment the key started checking it. That is the key testing something real rather than a regression.
- `gate2.py` reads the example's task table by column name now, after adding one column broke a reader that keyed on position. Two other tables in that file are still read positionally.
- **The tick chip on the edit screen reads the card, not `due_phrase_short`.** A title carries no date words, so re-reading one finds no date, and a stored task's short phrase is reachable only through a card. So an overdue task's chip reads `overdue` where a typed one would read `since Wednesday`: the card is the mobile sentence and collapses it, and the chip inherits the collapse. The clean version is a `due_phrase_short` on the card, which is a thirty-fourth rendered output and a contract change.
- **The matches on screen 2 are drawn ACTIVE before IDEAS, not strictly by tier.** `readResults` sorts globally and then partitions into two groups, and the screen draws the groups' rows one after the other with the headers off, per `MVP.md`. So an idea that matches exactly can sit below a dated task that shares one word. The fix is one flat list out of the engine rather than a second ordering written for the screen.
- **The 4 August deviation expired in `spec.md` and never in `gate2.py`.** It said the price of a config bump was a Stage 1 rewrite and a Gate 1 press, and session 92 declared it payable. Nothing charged it: `spec/example.md` kept a stamp two bumps behind the live config, the gate exempted the file by name, and the stamp sits in a table cell which the gate skips as illustrative anyway. Both fixed — the stamp reads the live config and the gate reads it by name, proven by putting the stale one back and watching it fail. The general shape is the third of these: a rule written where only a person can reach it reads as finished and is not.
- **`tsc --strict` was not run by anything.** `types.ts` declared `AlarmType` twice from session 93 — once widened at the top of the file, once still `"none"` at line 46 — and compiled with two `TS2300` errors while `spec.md` said it compiled clean. Fixed. The gap is structural and open: five commands run before and after every change and none of them is the compiler, so the machine contract is the one artefact with no check under it.
- **`weekend morning` reads `Due weekend morning`, and the band changes nothing.** A band is attached only to a named day or a spelled-out date, not to a span, a week or a month, because which morning of a weekend nobody said. The words still leave the title as a loser does, and `date_phrase` carries them, and the span branch of `readDuePhrase` reads `date_phrase` whole. That branch has been reading losers' words since spans were written; `weekend morning` is the first line that makes it visible.
- **The answer key says `chip_spans` is read by nothing, and it is read.** E7 and E8 hold identical `typed_line` and differ only in `chip_spans`, and they resolve to different dates: 3pm today against Friday. The claim is true of the four columns section E checks and false of the record, and E7's own note two paragraphs later says the opposite. A wording fix, not a behaviour one, and it needs his hand because it is the key.
- A hedge on a task due today, or on a band, has no screen trace: `Due around today` and `Due around this morning` say less than the plain forms, so the hedge lives only in `date_hedge` for those. Only a day, a date or a time carries the `around`.
- The answer key does not pin `duplicate.threshold`. Its scores leave a gap from 0.50, the highest that stays quiet, to 0.82, the lowest that fires; every value in between gives every verdict the key states. Moving the number from 0.8 to 0.6 changed no row. Pinning it needs a pair inside the gap whose right answer is known, and `call markan` against `call marken` at 0.75 is the shape of that case.
- The duplicate dialog's clause is written but unchecked. The key checks the dialog as present or absent, and every open task it hands in has no due date, so no case exercises the half of the sentence that names when the old task is due.
- The key hands `existing_tasks` two fields, `raw_text` and `normalised`, while the contract says every open task is handed in whole. The dialog names the existing task and falls back to `raw_text` because no title is supplied.
- A weekday that names today resolves to today rather than a week ahead. No case in the key covers it, so the choice is recorded here rather than evidenced.
- The date rule reads `now` and both Stage 3 harnesses were handing it an empty string, which is the second time a rule reading a new input has broken them. The pattern is a harness written against a placeholder that ignored everything.
- `gate4.mjs` reads its own pass condition from the stage line in this file, so an untrue stage line makes the runner report the wrong verdict rather than fail. The stage line is now load-bearing.
- The identifier check's alias list (`schema`, `key`, `types`, `cfg`) is hand-kept. The nouns themselves come out of VERSIONS and cannot go stale, but a versioned thing called something the alias list does not hold is invisible to the check, and only a reading finds that.
- The identifier check reads an all-caps run as one part, so `KEYA1` is invisible where `keyA1` is caught. No identifier in the bundle is spelled that way today.
- `working.decided_by` is empty on every line typed. It names what separates a row from its neighbour, and the line being typed is not on the list until it is added, so it has no neighbour. The contract says it is never empty in a list of two or more, which stays true: the cards each carry their own.

- `sort_header` is computed from the typed task's `list_header` rather than from the list on screen, so it appears and disappears with the line being typed instead of with the toggle. The same mistake the panel heading made, in the one place still making it.

- Four fields are collected and never shown: `est_duration_min`, `push_count`, `first_due_at`, and the day load built from the first. They shape the Ideas order, the ranking, and which push targets appear. The cost is stated once here: a suggestion whose reason is invisible is a suggestion nobody can correct, and when a push target looks wrong there will be nothing on the screen to argue with. Taken deliberately, at his call, and the LATER LIST's rule that a learning layer must stay traceable now applies to a system that already is not.

- `selftest.py` had a fixture matching `Twenty-\w+ objects` in `types.ts`, which stopped matching the moment the count reached thirty. It read as a hard failure rather than a stale pattern, the third time a fixture pinned something that moves. Widened to any number word.

- `alarm_lead_by_type` holds fourteen entries and every one of them is fifteen. It changes no behaviour until a number moves. That is deliberate and stated in the contract, but it means a config object exists that no case could distinguish from its absence: nothing in the key would notice if the whole table were deleted.

- The alarm lead is one number for every task, fifteen minutes, and a job that takes thirty is told too late to start it. The app cannot say so, because duration is a quiet field, so the person has to know to move the number. Chosen at his call over a per-type table; the alternative was another list fitted to nothing.

- The clash check runs on `est_duration_min`, which is a per-verb default rather than a measurement, so two tasks fifteen minutes apart collide because of an assumption nobody stated. Chosen over clashing on stated times alone, at his call. The cost lands twice: false warnings from guessed windows, and a warning that cannot explain itself, because the quiet-fields rule forbids it from naming the minutes it was computed from.

- The Ideas list is sorted by `est_duration_min` and nothing on a row shows it, so it is a list ordered by a number the reader cannot see. Newest-first was offered and declined; kept at his call, recorded here because it is the same shape as the objection that kept tabs out of Part A for eighty sessions.

- A query matching nothing draws a panel with two `(none)` lines rather than a sentence. No rule says what an empty search reads, the same gap as the empty task list.

- Search is checked by reading the screen, like the ranking. No case in the key names `results`, and the four tiers could be reordered with every gate still green.

- `type_order` is fitted to nothing and the key does not pin it, the same standing as `duplicate.threshold` and `limits.raw_text_min_chars`. It was reordered on a stated principle, which makes it arguable rather than arbitrary, and no more evidenced than before. Left unpinned at his call: pinning a guess makes it harder to correct.

- The ranking is checked by reading the screen. No case in the key names `cards`, `rank_key` or `decided_by`, so the nine factors could be reordered and every gate would still pass. The example's four-row ordering is the only worked example, and it is prose rather than a case.

- The badge the engine draws is not the badge the example drew. The contract's template is `action_verb · est_duration_min m` and the four hand-drawn cards pad the number three different ways, one of them with a stray space. The engine follows the template; the drawing is the illustration. Gate 6 has to reconcile them, and until then the app and section 1 disagree by whitespace.

- The panel heading was the typed task's destination rather than the name of the list under it, so it read `Ideas` while the Default list sat below. Invisible while the cards were constants. The renderer takes the heading as an option now and `list_header` keeps its own meaning, but no rule says which list a screen is drawing, because Part A has always assumed one.

- `existing_tasks` is documented as every open task handed in whole, and the answer key hands in two fields per task. `cards.js` skips a task with no title, so the key's cases build no cards and stay green. The gap is in the key rather than in the engine, and closing it means writing whole records into section D.

- The empty capture panel is drawn by the renderer but reached by no harness. `resolve()` refuses an empty line, so `check_render.mjs` can only check the typed panel. The screen supplies `chip_row` and `significance_row` from config without asking the engine, and that path is Gate 6's.

- Two Example cells in the contract disagreed with rules the contract itself states: `card_title` kept the marker word, and `significance_row` emphasised nothing. Both were corrected by reading rather than by any gate. `gate2.py` checks shape and counts in those tables, never whether a cell obeys the rule beside it.

- Earlier records call the answer key an 80-case key; it declares 88. The count is now stated once above, and `gate2.py` reads the real one out of the key.

- Part B carries a constraint from Part A's window rule: it must decide chase behaviour from `date_firmness`, not from `deadline_band`. If it ever chases on band alone, every window-dated task pesters the user from its midpoint. Recorded now so it is not rediscovered later.

- **A reboot mid-chain restarts the five autos, and a phone that reboots every chain never escalates.**
  His decision: the spent autos do not survive. So a reboot at 17:00 during a chain that started at
  16:45 rings again until about 17:35, which is the harmless half. The other half is that a phone
  rebooting during every chain never reaches the end of one, so `alarm_unanswered_at` is never written
  and the task never rises. That failure is silent, which is the worst kind, and it is recorded rather
  than guarded against.

- **`reminder_fatigue` now has two writers with opposite meanings.** Part A writes it as "alarms that
  went unanswered", ranked descending: more failures, higher. Part B reserved it as notification
  history, where the intent was to back off from a person who has been told too often, which ranks the
  other way. Both cannot be true of one column. This is the one field in the alarm design that must be
  re-read when Part B arrives, and it is named here so that re-reading is not a discovery.

- **The R2 palette is stated a second time, in Kotlin.** `AlarmActivity` cannot read a stylesheet, so
  ink, paper, signal and signal-deep are colour literals in it. Every other duplicated constant in this
  project has drifted eventually. Nothing checks these two against each other.

- **An alarm set on a task whose time is later removed stays set.** `alarm_type` remains `on` while
  `has_time` goes false, and `canAlarm()` is what stops it ringing. So the record holds a request that
  cannot be honoured. Reading the gate rather than clearing the field is deliberate, because an edit
  that finds no date leaves the date alone and clearing the alarm would make a save reset something the
  person set.

## DECIDED, NOT BUILT — Part C's workflow

Every line here was settled on 17 August and none of it is in the schema yet. It is written down
because a decision that lives only in a conversation reads as finished and is not, which is the third
thing in this file to say that sentence.

**One table, not two.** No `cascade_edge`. A task's whole readiness condition sits on its own row, so
joining or leaving a workflow is an edit to one field and deleting a task takes its conditions with it.
Six columns on `cascade_task`, all defaulted, so existing records are untouched:

| Column | Holds |
|---|---|
| `waits_for` | `jsonb` array of `{ task, kind, on, delay_min }`, default `[]` |
| `condition_label` | the question a branch turns on. Its presence is what makes `if` / `else` legal |
| `condition_met` | `true` / `false` / null. Null means nobody has answered yet |
| `loop_limit` | how many times a declared loop may run before it is exhausted |
| `loop_count` | how many times it has, default 0 |
| `workflow_id` | one grouping id. Not both this and `project_id`; two ids for one grouping is the shape every duplicate defect here has had |

**`kind` is per dependency, not one mode on the receiver.** `and` / `or` / `if` / `else`. A single
`gate_mode` cannot express a task that branches, and `if` / `else` are unavoidably per edge. Readiness
reads: every `and` met, at least one `or` met where any exist, an `if` met when the upstream closed and
`condition_met` is true, an `else` met when it closed and `condition_met` is false.

**`on` is the trigger:** `done`, `overdue`, `cancelled`. An overdue trigger fires from a clock Part A
is not running, so it waits for Part B's scheduler rather than being evaluated lazily on render.

**`delay_min` from the start.** One integer, and it is what a TIMER operator would have been. LOOP is
`recurrence`, which already exists. Two of the six operators disappear into fields already owned.

**A missing upstream counts as satisfied.** Postgres cannot foreign-key inside `jsonb`, so a dangling
reference is possible. The alternative hides the downstream task for ever with nothing on screen.

**A blocked task disappears from every list** until it is ready. His call, over showing it marked.

**An undeclared cycle is refused at save**, by a walk over the cache from the new dependency's target.
A declared loop is not a cycle and is legal: a back edge needs an upstream carrying a
`condition_label`, a `kind` of `if` or `else`, and a `loop_limit` on the loop's entry task. At
`loop_count == loop_limit` the back edge is unmet for ever and whatever waits on the exhausted loop
becomes ready. Nothing can enter the store carrying an undeclared cycle, so no task hides behind one.

**Same-project is enforced when the dependency is added**, not when readiness is computed.

**`blocker_ref` stops being the authority.** One `Uuid` cannot hold three upstreams. `blocked`,
`blocker_reason`, readiness and `workflow_position` become working values recomputed from `waits_for`,
the way `deadline_band` already is.

**Marking done a task that carries a `condition_label` asks the question first**, two buttons, no way
past it. Somebody has to answer or both branches stay invisible. That is a third mandatory dialog and
D-1's closed exception list is currently two: D-1 is scoped to capture and this is a completion, so the
wording does not reach it, and the list is longer in writing rather than silently.

**A drag onto an hour on the week grid is allowed, and is a push:** it sets the date field, touches no
words, and sets `has_time` only when the drop lands on an hour rather than a day column.

**What it costs, recorded rather than discovered.** Finding what waits on a task is a scan of every
row, free at this size and not for ever. Two devices each adding a different upstream to one task
collide under newest-wins and one addition is dropped silently, where two rows in a table would both
have landed. That is the price of the single table and it is accepted.

## LATER LIST

- Reading a duration typed into the line. `call kushan 30 min` keeps the default and the words stay in `title`. The decision was to add nothing to config for a parser that does not exist; the case is in the key and states the current behaviour, so writing the rule later is a value change with a row already watching it.
- Learning layer for duration and context suggestions. Permitted only if its output is config-shaped and traceable ("your last 8 calls averaged 12 min"), never an opaque score.
- Batch response, grouping by the verb's batch key.
- Undo as a stack rather than single level.
- The other 24 responses.
- Parser languages beyond English. The verb lexicon takes transliterated tokens from day one; date grammar does not.
- Two-way calendar integration.

## DECISION LOG (append only, never edit or delete)

- 1 Aug 2026 — Split the project into Parts A, B, C, D by what must already be true before each can work — Part B's constants cannot be set without real capture data, so building it first means tuning against an empty database — a working reminder is further away than it looks.
- 1 Aug 2026 — Prior documents (v1.0 PDF, SDS, older decision files) are reference only; nothing is used from them unless explicitly named — two spellings of one concept is Principle 2's failure mode, and the old files carry camelCase against this project's snake_case — some earlier reasoning has to be re-derived rather than inherited.
- 1 Aug 2026 — snake_case everywhere — one concept, one spelling, forever — nothing.
- 1 Aug 2026 — Wrote the Stage 1 example before further build work rather than accepting the deviation as debt — the Part A spec had been written spanning Stages 2 to 6 with no finished example beneath it — one session spent on a document instead of code.
- 1 Aug 2026 — One text box does capture, search and edit — search results appearing while typing catch near-duplicates before they are created, which handles 45% of real input without a dialog — the box has two states and a bound state that must be unmistakable, or it silently overwrites tasks.
- 1 Aug 2026 — Binding is entered only by tapping a search result, and left only by save, empty field, or ✕ — every other path risks editing a task while believing you are capturing one — no keyboard-only edit path.
- 1 Aug 2026 — Duplicate handling is live search plus an exact-match "Add anyway"; no merge flow, no Jaccard threshold — a warning after you commit reads as correction, seeing the task while typing just redirects you — near-duplicates with different wording will slip through.
- 1 Aug 2026 — Importance is asked at capture, three buttons, low 10 / normal 30 / high 70 — with dates on nearly everything, importance is the user's own lever on ordering and the engine must never write to it — one more control in the capture row.
- 1 Aug 2026 — `date_origin` (typed vs chip) is dead. Replaced by `date_precision`, `date_firmness` and `date_anchor`, derived from the expression rather than the route it took — a chip tap and a typed word carry the same intent, so route was never the signal; the words are — three fields where there was one.
- 1 Aug 2026 — Precision and firmness are separate rankings — "first week of August" and a picked 5 August differ in width but not in bindingness, while "by 5 Aug" and "5 Aug" differ in bindingness but not width — twenty-one combinations to reason about instead of six.
- 1 Aug 2026 — Store the input word and the derived level side by side (`date_phrase`, `date_hedge`, `date_marker` alongside the three levels) — different words produce the same level, and since stored data is never rewritten the word would be lost permanently — three more columns.
- 1 Aug 2026 — Stored data is never rewritten retroactively; a config change affects future captures only — the user has already seen and accepted what is on screen, and rewriting it is the engine changing data without a tap — an improved lexicon never fixes old records.
- 1 Aug 2026 — Every classification enum carries fallback members, so no derived field is null — a null forces a check at every consumer, and `count(other)/total` is a free accuracy metric — `other` silently absorbs failures unless someone looks at the count.
- 1 Aug 2026 — `other` and `undetermined` stay separate — one is a lexicon gap and fixable, the other is unclassifiable input and is not — one more enum member.
- 1 Aug 2026 — Time bands are ranges, not points. Inside the band, due lands at the midpoint and never before `now` — typing "morning" at 10:40 means this morning, not tomorrow, and should not create an already-overdue task — one extra config value per band.
- 1 Aug 2026 — Window expressions write `earliest_start` at the window start and `due_at` at its midpoint — a window names both, and the midpoint makes the task rise before the window closes — the task enters the overdue band with part of its window still to run, which is intended.
- 1 Aug 2026 — Marker words split by ambiguity: strong markers count anywhere, weak markers only next to a temporal expression — "deadline" and "cutoff" cannot mean anything else, while "by" and "before" are two of the commonest prepositions in English — two lists to maintain instead of one.
- 1 Aug 2026 — Ranking is lexicographic over ten factors: `deadline_band`, `is_hard`, `importance`, `date_firmness`, `date_precision`, `commitment_type`, `est_duration_min`, `workflow_position`, `reminder_fatigue`, `created_at`; `pinned` overrides all — a weighted sum produces positions nobody can explain, which fails the acceptance rule — ten factors to reason about, several inert until Parts B and C.
- 1 Aug 2026 — A hard deadline outranks importance; importance outranks everything below — a user marking something high should beat inferred signals, but not a real deadline — a high-marked trivial task still sits above a soft-dated important one.
- 1 Aug 2026 — Hedges affect `date_firmness` only, never importance — importance is a user tool and the engine may not write to it — a hedge on a dateless task is stored and does nothing.
- 1 Aug 2026 — Ideas get full inference including duration — duration is a property of the work, the due date a property of the commitment, and they are independent — Ideas becomes sortable by duration, which is what stops a someday list dying.
- 1 Aug 2026 — Undo is single level, stored as a row with `{action, task_id, prior_state, created_at}` — a row upgrades to a stack by changing a limit; a variable would need a rewrite — only the most recent action is reversible.
- 1 Aug 2026 — The undo button expires in the UI; the entry persists in the engine until superseded — a mis-tap is noticed immediately, but a bad edit is noticed later — an expired-undo affordance has to exist somewhere.
- 1 Aug 2026 — Capture confirmation is the box going empty — same signal as sending a message, needs no new UI — nothing.
- 1 Aug 2026 — `normalised` derives from `title`, after every structured expression is removed — "Call markan morning" and "Call markan tomorrow" are the same task and must recognise each other — two genuinely different instances of a recurring task look identical and need "Add anyway".
- 1 Aug 2026 — Ids are client-generated UUID v7, handed into the engine as an input — Part 5 forbids the engine generating identifiers and offline-first forbids waiting for a server; v7 is time-ordered so it indexes and sorts naturally — nothing.
- 1 Aug 2026 — Timezone: UTC storage, local display. Anchors and band boundaries are local — deciding this later makes every stored time suspect — nothing.
- 1 Aug 2026 — Projects are created manually only; no auto-grouping and no suggestion — structure the user did not ask for is structure they cannot predict — 15 of 45 real tasks cluster on three shared tokens and arrive as orphans.
- 1 Aug 2026 — Parts B, C and D each get their own Stage 1 example at their turn — a whole-app example fails Gate 1 today because Parts B to D have no written rules — three more Stage 1 passes later.
- 1 Aug 2026 — D-1 for Part A is "Typing the thought is the whole of the work" — it is a single claim rather than a list, and it names the thing that separates Part A from a notes app, which is fast at everything and structures nothing — it forbids every mandatory post-capture step, so no field, prompt or dialog may ever be required.
- 1 Aug 2026 — D-1 governs the normal path; the duplicate and split dialogs are kept as a closed list of two exceptions — both fire only where the engine cannot determine how many records the input describes, which inference cannot answer and which writes wrong records if guessed — D-1 stops being an absolute, so the exception list has to be policed or it will grow.
- 1 Aug 2026 — Window expressions are clipped to `max(stated_start, now)` before the midpoint is taken, replacing the floor rule — "this week" said on Thursday should mean Thursday to Sunday, not a Monday-anchored midpoint already in the past — `due_at` for a window now depends on when it was captured, so two identical phrases typed a day apart resolve differently.
- 1 Aug 2026 — The reconstruction invariant is scoped to typed input; chip labels do not participate, and typed-versus-tapped is derived by substring rather than stored — a chip label was never in `raw_text`, so there is nothing for it to have dropped — the check is silent on chip captures.
- 1 Aug 2026 — `title` strips only `date_phrase`; `normalised` derives from `raw_text` minus every structured span — display and matching want different strippings, and `GST filing` alone is unreadable — markers and hedges are duplicated between `title` and their own fields.
- 1 Aug 2026 — The list badge shows the verb, blank when `undetermined` or `other`; never the commitment type — verb says what you would do and drives batching, type is abstract — one more lexicon token needed per noun form, starting with `application`.
- 1 Aug 2026 — One fallback vocabulary of three: `other` (lexicon gap), `undetermined` (tried and failed), `none` (not applicable) — collapsing them hid two real conflations in `context` and `date_precision` — one more enum member on several fields.
- 1 Aug 2026 — Fuzzy matching is in Part A, in search results only, never triggering the duplicate dialog — transliterated names typo inconsistently and a fuzzy tier is the only thing that surfaces them, but a fuzzy match is far too weak to interrupt anyone with — false positives in results, which cost nothing because results are non-blocking.
- 1 Aug 2026 — `deadline_band`, `is_hard`, `workflow_position` and `reminder_fatigue` are derived at read time, never stored, and belong under the contract's working values — `deadline_band` changes at every midnight and a cached copy goes stale silently — four rows in the contract that have no column behind them.
- 1 Aug 2026 — A fully passed daily band rolls to its next occurrence, unclipped; it does not collapse to `now` and does not refuse to parse — nobody typing "morning" at 13:00 means this instant, and refusing discards a clear temporal intent — window resolution now has three cases rather than one rule.
- 1 Aug 2026 — `is_hard` is a working value, not a column; the stored field count is 33 — it is a pure function of `date_firmness` and storing it would let the two disagree — one more row in the contract with no column behind it.
- 1 Aug 2026 — `date_precision` tail order is open, none, undetermined, with the broken-parse case last — a total order avoids undefined sorting, and a parse failure should sink rather than float — the tail is currently unreachable, since all three imply no `due_at` and route to Ideas.
- 1 Aug 2026 — `commitment_type` carries no `other`; `action` is its legitimate default — a task matching no other type genuinely is a plain action, and the classification failure signal already exists upstream in `action_verb` — type accuracy cannot be measured on its own field.
- 1 Aug 2026 — `earliest_start` records the start of the window `due_at` belongs to, not the stated window start — a passed band rolls `due_at` to the next occurrence, and an unclipped stated start left the two fields describing different days — "the stated start" is no longer recoverable when a band rolls.
- 1 Aug 2026 — A `day`, `month` and `span` are windows and need no separate resolution rules; `time` is the only precision level that is not a window and resolves to the stated instant — one rule covering six of seven levels beats six rules — the day window's bounds (`day_start_anchor` to end of day) are now load-bearing for every picked date.
- 1 Aug 2026 — `span` survives as a distinct precision level — the Weekend chip produces it, so it is reachable even though no capture on the example exercises it — a level defined and untested until a weekend capture is added.
- 1 Aug 2026 — Part 7's start-of-session report is adopted from Session 5 onward — three sessions of mechanical findings survived because nothing checked the document against itself before work began — one turn per session spent reporting before anything is written.
- 1 Aug 2026 — Every window is half-open, `[start, end)`; "end of day" is 24:00 exclusive, never 23:59 — half-open intervals tile without gaps, and 23:59 shifts every midpoint by half a minute, which Stage 4 boundary cases would surface as a failing test — one convention that must hold across bands, days, spans, weeks and months without exception.
- 1 Aug 2026 — `day_start_anchor` is 09:00 and the morning band starts from it by reference rather than repeating the value — the coupling then lives visibly in config instead of being a coincidence between two keys — shifting the day start moves every picked date's midpoint too; separating them is a config change if that is unwanted.
- 1 Aug 2026 — The badge is blank when the verb is `undetermined` or `other`; the enum name is never shown — `undetermined · 30m` exposes an internal value to the user, and the alternative of falling back to commitment type reintroduces the badge mixing that finding 7 removed — a row with one token beside rows with two.
- 1 Aug 2026 — Rule 8's 400-line cap is code-only. Documents have no line cap and are bounded by one document per named deliverable; a document needing a split means the stage holds more than one deliverable — Principle 3 is about narrowing where a fault can live, which is logic rather than prose, and splitting a spec risks Principle 2 instead — specs can grow unbounded between deliverable boundaries.
- 1 Aug 2026 — Every window's bounds are stated in one table: band, day, span, week and month all start at `day_start_anchor` on their first day and end at 24:00 exclusive on their last — leaving them implied produced a week midpoint that no stated convention reproduced, which failed Gate 1 outright — five bounds to keep in step with any change to `day_start_anchor`.
- 1 Aug 2026 — An explicit `end` or `start` anchor overrides the midpoint rule; only `window` anchors use it — the point of marking a deadline is that it is not the middle of anything — `by Friday` and plain `Friday` now resolve to different instants on the same date.
- 1 Aug 2026 — `end` resolves `due_at` to the window end minus one minute — at minute resolution that is the last instant inside a half-open window, keeping the date correct and the ordering late — a value that looks like a magic 23:59 unless the convention above is read first.
- 1 Aug 2026 — The `end` marker set is exactly the union of the strong and weak marker lists; no deadline marker sits on a start anchor, and `latest` joins the strong list — a third vocabulary would drift from the two that already exist, which is Principle 2 — start markers are the only markers that set an anchor without touching firmness.
- 1 Aug 2026 — A midpoint may land outside working hours, as the weekend's Sunday 04:30 does, and no rounding rule is added — `has_time` is false for every window resolution, so only the date is consumed by display and by `deadline_band` — a stored instant that reads oddly if anyone ever renders it with a time.
- 1 Aug 2026 — `date_anchor` is a resolution strategy, not a label on the commitment, and its five members are evaluated in order: `none`, `point`, `end`, `start`, `window` — a total ordered function has no collisions, which is what `GST filing deadline` and `by 5pm` were exposing — `end` no longer survives on a dateless line, so the deadline is carried only by `date_marker` and `date_firmness`.
- 1 Aug 2026 — An `end` anchor leaves `earliest_start` empty — "by Friday" means any time up to Friday, and writing Friday 09:00 says work may not begin until then, which Part C would turn into hiding the task until the morning of its own deadline — `by Friday` and plain `Friday` now differ on both fields rather than one.
- 1 Aug 2026 — `needs_confirmation` never fires in Part A — low confidence resolves to a fallback member instead of prompting, which is what "nothing is mandatory" means in practice — wrong inferences accumulate silently until someone reads the list.

- 1 Aug 2026 — `date_anchor` evaluation order becomes `none`, `end`, `start`, `point`, `window`, with the marker rows branching on time versus window — a time supplies the value and a marker supplies the strategy, and the old order converted every start marker carrying a time into a deadline — `by 5pm` and `at 5pm` now store different anchors while resolving to the same instant.
- 1 Aug 2026 — Row 1 keys on `date_precision` being `none`, `open` or `undetermined`, not on the absence of a temporal expression — `someday` and a failed parse are expressions without bounds, and they reached the midpoint rule with no window to take a midpoint of — the anchor rule now depends on the precision level rather than on raw input.
- 1 Aug 2026 — `has_time` follows the presence of a time, independent of the anchor — it was effectively `anchor == point`, which the reorder breaks, since `by 5pm` and `after 5pm` both carry a time under other anchors — one more field whose origin has to be stated on its own.
- 1 Aug 2026 — A bare passed time rolls to the next day; a time attached to a date does not — recurrence is the same test the third window case already uses, and a user who named a specific past coordinate should keep it — a user who meant today loses the task for 24 hours, carried only by the parsed-date chip showing the resolved value before commit.
- 1 Aug 2026 — A line carrying both a deadline and a start marker resolves as `end` and drops the start bound — `date_anchor` is single-valued and the deadline is the stronger commitment — `after Monday before Friday` loses its start bound even though `earliest_start` is empty and could have held it.

- 1 Aug 2026 — `span`, `day`, `month` and `time` are covered by a resolution table rather than by new captures — a capture needs a task line, and any line invented for the example would also invent the lexicon and config entries behind its verb, type, context and duration — the four levels have worked date values but no card and no full record, which moves the gap from Stage 2 to Stage 4.
- 1 Aug 2026 — Stated that `due_at` can carry a different date in UTC than locally, and that every date comparison reads the user's zone — the `Weekend` and `next month` midpoints both land at 04:30 local, which is the previous UTC day — nothing, this was already true and merely unwritten.

- 1 Aug 2026 — Timestamps are stored local with an offset, `2026-08-09T04:30:00+05:30`, rather than as UTC — local is what the user said and what the card shows, and the offset removes the guess without making UTC the primary reading — an offset is not a zone, so a DST-observing zone will need a zone name instead, which is a Part D change.
- 1 Aug 2026 — `date_firmness`'s middle member is renamed `firm` to `normal` — `hard` is earned by a marker and `soft` by a hedge, so the middle is what remains when neither fires, and `firm` made it claim a property no input supplied — nothing, the enum is the same size and the derivation is unchanged.
- 1 Aug 2026 — No `firmness_source` field — it would be a pure function of `date_firmness`, which puts it in the working-values table beside `is_hard` rather than in the record — the field becomes necessary if a user can ever set firmness directly, since `normal` would then mean either unmarked or chosen.

- 1 Aug 2026 — `action_verb` is a closed set of canonical members and the matched span is stored beside it as `verb_phrase` — `Reply` canonicalises to `message` and `file` to `submit`, so storing only the member loses what was typed, which is the same split `date_phrase` and `date_precision` already use — one more field, 34.
- 1 Aug 2026 — The lexicon matches nouns as well as verbs — `application` gives `submit` and `software` gives `make`, which is why half the list badges exist at all — a noun with no entry gives `undetermined`, so badge coverage tracks lexicon coverage rather than grammar.
- 1 Aug 2026 — `type_source` records `derived` or `user` — the type chip can be tapped, so `action` alone cannot say whether it was defaulted or chosen — one more field, 35. This closes the Stage 2 item carried from Session 10.
- 1 Aug 2026 — `updated_at` added, moving on every change including Done — §6 already edited a task and no field recorded it — one more field, 36. An undo restores the previous value along with everything else it reverses.
- 1 Aug 2026 — Ranking factor 10 becomes `updated_at` descending, comparing absolute instants — editing a task means the user is thinking about it, so it rises among the things it ties with — a list sorted by another key reorders when a tied item is touched, which shows most in Ideas where defaulted durations tie often.
- 1 Aug 2026 — One typed sentence is always one task; the split dialog is removed — the engine cannot tell three payments from one payment described three ways, and splitting writes records the user did not ask for — the permitted-dialog list drops from two to one, and comma items now sum the duration instead, with `duration_source = summed`.
- 1 Aug 2026 — The duplicate dialog fires on trigram similarity at or above `config.duplicate_similarity_threshold` = 0.90, guarded by `config.duplicate_min_chars` — sentences rarely match exactly, and an exact match is the 1.00 case of the same rule — near-duplicate transliterated names now interrupt at capture, reversing the earlier decision that the fuzzy tier never triggers the dialog.
- 1 Aug 2026 — Merging two records is not a Part A operation — the way to reach an existing task is to tap it in the live results, which binds it for editing — `Add anyway` leaves two records and the only remedy is to delete one.
- 1 Aug 2026 — `importance` renamed to `significance` throughout — the word read as a ranking weight when it is a user control the engine may never write to — 14 occurrences changed, no behaviour.
- 1 Aug 2026 — Part A commits only to preserving the inputs a zone re-derivation would need, and does not re-derive — nothing fires in Part A, so whether a band follows the user across zones is Part B's decision — `has_time` is an imperfect proxy for "my day versus a coordinate", and `gym at 7am` and `call markan in the morning` both sit on the wrong side of it.

- 1 Aug 2026 — Duration summing runs only on a comma list after a single verb — the removed split dialog carried that condition and the sum inherited nothing, so every comma anywhere multiplied a duration — the guard is the one part of the split rule that survives its dialog.
- 1 Aug 2026 — A line with no lexicon match gives `action_verb = other`, not `undetermined` — once the lexicon matches nouns, no match means the lexicon lacks the token, which is a coverage gap — `undetermined` becomes unreachable on `action_verb`, and the field carries its real members plus `other` only.

- 1 Aug 2026 — `completed_at` becomes `closed_at`, `task_state` is `ready` / `done` / `cancelled`, and `archived` is a separate boolean — done and cancelled are both outcomes and both deserve a time, while archiving is a decision to stop looking rather than an outcome — one more field, 37, and an archived task keeps whatever outcome it had.
- 1 Aug 2026 — `duration_defaults.other` is 5 minutes — a task the lexicon cannot classify is one the engine knows nothing about, and a low guess floats it up the Ideas duration sort rather than burying it — `Srilanka tickets` and `Kena investment` move from 30m to 5m and the Ideas order changes.
- 1 Aug 2026 — `duration_source` members are `default`, `selected`, `learned`, `summed` — `typed` is dropped because it needs a duration lexicon that does not exist, and `suggested` is not a source since leaving a suggestion alone is `default` — `selected` is unexercised in Part A until the duration control is built.
- 1 Aug 2026 — Enum members live in config, so config-resident enums are branded strings validated at runtime rather than TypeScript literal unions — adding a member must be a config change, not a code change — the compiler cannot catch a bad member, so runtime validation is load-bearing.
- 1 Aug 2026 — A record is read against its own `config_version`, never the current one — a config that renames or drops a member would otherwise silently reinterpret every record written before it — config versions become append-only, and every read path has to carry a version.
- 1 Aug 2026 — At write time a value outside the contract is rejected, never coerced — coercion hides an engine defect and writes something the user did not ask for — a bug surfaces as a failed capture rather than a wrong record.

- 1 Aug 2026 — Config splits into vocabulary, lexicon and behaviour, one object per thing that grows, 22 in total — only vocabulary members are held inside records, so only vocabulary needs protecting, and putting the lexicon under the same rule would make every new word a vocabulary generation — 22 objects to keep in sync rather than one file to edit freely.
- 1 Aug 2026 — Every vocabulary member carries `active`; removal is forbidden and deactivation replaces it — a deactivated member stops being offered and written while every record holding it still reads — a member added by mistake is permanent, just inactive, which is the reason to keep `a.1`'s vocabulary thin.
- 1 Aug 2026 — `config_version` stays `<letter>.<integer>` although no operation now bumps the letter — deactivation removed the only case that would have, and the letter is reserved so a forced uninterpretable change is identifiable by version string alone — a format carrying a case that does not yet exist.
- 1 Aug 2026 — Duplicate detection is `max(trigram, word_match)` above 0.6 with numeric tokens excluded wherever they sit — neither measure alone separates a transliterated typo from a trailing identifier, and excluding numeric tokens is what makes `file form 8` and `file form 9` distinct rather than similar — the rule is a guess chosen against seven pairs, with no backlog behind it.
- 1 Aug 2026 — `a.1` ships 7 `action_verbs` and 2 `contexts`, drawn only from what the example exercises — a missing member falls to `other`, which costs nothing, while an extra member is permanent — all 14 `commitment_types` ship anyway, so the thinness is inconsistent and the `active` flag is what makes it recoverable.

- 1 Aug 2026 — The contract is rebuilt in the protocol's three groups: Inputs, working values, outputs saved and shown — the previous version answered what each field is and never asked where a value comes from or where it goes, which left every rendered string and both handed-in values with no named home — the 37 stored fields become one part of Outputs rather than the whole contract.
- 1 Aug 2026 — `now` and `new_id` are inputs handed in on every call, not read from a clock or generated inside the engine — a resolver that reads its own clock cannot be pinned by a golden case, and Stage 4 needs the output to be a pure function of the inputs — every call site carries them.
- 1 Aug 2026 — `compare_key` is named: `normalised` with every purely numeric token removed — the duplicate rule was operating on an unnamed intermediate, so the value the comparison actually runs over had no contract home — one more working value.
- 1 Aug 2026 — `UndoEntry` enters the contract with `prior_state` as a whole `Task` rather than a diff — Part 3 Stage 2 fixes the saved shape, and an undo restores the previous `updated_at`, which only a full copy can do — a second saved shape to migrate if the record changes.
- 1 Aug 2026 — `timings.undo_sec` is renamed `undo_ui_timeout_sec` to match `spec/example.md` — Part 4 forbids one concept under two names — `timings` disappears as an object, leaving 21.
- 1 Aug 2026 — `hedge_words` drops to `maybe` alone and `verb_lexicon` drops `build` and `payment` — those entries were invented to make `a.1` look complete, which is the fault this project exists to prevent — a surface form the example does not show now resolves to `other`, which costs nothing.

- 1 Aug 2026 — Numeric-token stripping becomes a suppression test run before the score, not a normalisation feeding it — stripping makes `file form 8` and `file form 9` identical, so scoring the stripped keys gives 1.00 and fires the dialog every time, which inverts the intent — one more working value, `numeric_variant`.
- 1 Aug 2026 — `duplicate.threshold` moves from 0.6 to 0.8, comparison `>=`, with both measures defined as Sørensen–Dice — at 0.6 `call raj` and `call ravi` score 0.74 and interrupt each other, which for transliterated names would be constant — the rule is fitted to eleven hand-made pairs and `gst filing deadline` landing on exactly 0.80 is luck rather than design.
- 1 Aug 2026 — `normalised` derives from `raw_text` minus every structured span, not from `title` — `title` keeps markers and hedges for readability, so deriving the duplicate key from it would compare text the user can see rather than text that identifies the task — recorded late; the reversal happened in Session 4 and lived only in the example's closed-items table until now.

- 1 Aug 2026 — Six more rendered outputs are named: `nav_bar`, `add_button`, `significance_row`, `type_chip`, `bound_task_chip`, `action_row` — Gate 2's first pass found them drawn in the example with no contract item behind them — the shown-outputs group goes from 8 to 14.
- 1 Aug 2026 — `significance_buttons` holds `{value, label}` pairs rather than bare numbers — the labels Low, Normal and High are on the example and had no home, and putting them beside their values keeps config at one object rather than adding a parallel list — a shape change to an existing config object.
- 1 Aug 2026 — `nav_bar`'s members are code, not config — a tab needs a screen behind it, so config could otherwise name a screen that does not exist — adding a screen is a code change by definition.

- 1 Aug 2026 — **Reverses** two earlier entries, quoted here as Part 11 requires. The first read: *"Ranking factor order settled: `is_hard` at 2, `significance` at 3, `updated_at` at 10."* The second read: *"Factor 10 becomes `updated_at` descending."* Both are superseded. Requested directly: *"The order of tests is wrong. Hard deadline beats all. I might be changing this list with a multiplier later, with hard deadline beating the multiplier. Make sure there is space for that."* Confirmed after the overdue consequence was put in writing.
- 1 Aug 2026 — Ranking becomes three tiers: overrides `pinned` and `is_hard`, a `mode`, then nine factors — a hard deadline is a promise to someone else and must beat any score, including one a later weighted mode produces, so it cannot sit inside the factor list — the blast radius is wider than one afternoon: `by` and `before` are weak markers, so `File the GST return by 15 December` is `is_hard` and outranks `file form 8`, overdue since Friday. That is accepted because the Default list is filtered before display, and the filter arrives with Part C. Until it does, a far-off hard task sits near the top.
- 1 Aug 2026 — `card_reason` is composed rather than named per shape: `due_phrase` plus `reason_clauses` trailing templates keyed by `decided_by`, both rendering when both apply — four separately named strings cannot carry the invariant that the sentence and the sort order never disagree, and one generated sentence can — `card_reason` needs the ranked list rather than one record, so its Stage 4 cases are list-shaped.
- 1 Aug 2026 — `due_phrase` is named as a working value taking `due_at`, `date_precision`, `has_time` and `now` — "this morning" is not a `deadline_band` member and was an unnamed intermediate, the same fault `compare_key` was named to fix — it goes stale at midnight, which is why it is a working value.
- 1 Aug 2026 — `bound_task_id` is an input; `add_button`, `input_field` and `bound_task_chip` are each a function of it — three signals that must agree become three views of one value and cannot disagree, and a golden case can set up an edit without replaying a tap — the engine holds no state between calls.
- 1 Aug 2026 — `trigram` is Sørensen–Dice over trigrams as a **multiset**, stated explicitly — set semantics give 0.73 rather than 0.71 on `gst filing deadline`, which crosses the 0.80 threshold — an unstated convention was deciding a boundary case.
- 1 Aug 2026 — Gate 2 is a script that reads every count from the files rather than trusting a prose number, and walks all five groups — the previous script checked `Task` and `Config` only, and passed while `Inputs` and `WorkingValues` were out of sync — the gate is now only as good as the script, which is a thing that can itself be wrong.

- 1 Aug 2026 — The tab bar is dropped from the example; Part A has two lists, `Default` and `Ideas` — `Today`, `Upcoming`, `Projects` and `Settings` are lines of text with no rule behind them, and Gate 1 forbids that — `nav_bar` leaves the contract, and the ordering question the tabs were hiding surfaces: the Default list is unfiltered until Part C.

- 1 Aug 2026 — `nav_bar` becomes `list_header`, one of `Default` or `Ideas` — Part A has exactly two routes a capture can take and both have stated rules, while `Today`, `Upcoming`, `Projects` and `Settings` were drawn with nothing behind them — the tabs and the filtering they implied move to Part C, and the `Default` list is unfiltered until then.
- 1 Aug 2026 — `due_phrase` renders at the finest granularity available: a stated time, then `date_precision` when the task is due today, then `deadline_band` — `has_time` was declared an input to `due_phrase` and no template consumed it, so a 5pm task read "Due today" and lost the only precise thing the user said — `reason_clauses.lead` gains a tier and `none` stops being ambiguous between the two vocabularies.
- 1 Aug 2026 — Each trailing clause carries its own joiner — `is_hard` contributes a sentence joining with `". "` and `significance` a clause joining with `", and "`, so a single joining rule cannot produce both sentences the example already shows — punctuation lives in config with the words it punctuates.
- 1 Aug 2026 — `trailing.significance` reads `you marked it <significance_label>` — hardcoding "high" would have made a card claim high when significance separated 30 from 10 — the label comes from `significance_buttons`, so renaming a button changes the sentence.
- 1 Aug 2026 — `decided_by` names the test separating a task from its neighbour: the row below, or for the last row, the row above — it was defined as empty for the last row while §1's table gives the last row a factor — every row in a list of two or more now carries one.
- 1 Aug 2026 — `gate2.py` is rewritten to discover rendered strings rather than match a fixed list, to compare the shown group against `types.ts`, to check each item's stated Example against the example text, and to read `spec.md`'s own version and count claims — the previous script passed while three outputs were spelled two ways, because it built the shown group and never compared it — the gate is only as good as the script, and the script is now the thing to review.

- 1 Aug 2026 — `action_verbs` goes from 7 to 18 at `a.2` — 152 of 200 real backlog lines fell to `other` under `a.1`, which made three-quarters of an answer key unwritable — every addition is permanent, and 8 of 10 domain cases still resolve to `other`. **This entry is a repair.** Session 25's version-stripping regex rewrote it, and it originally claimed 45 lexicon entries against an actual 14; see the Session 25 entry below.
- 1 Aug 2026 — `tickets` and `investment` stay out of `verb_lexicon` deliberately — they are the example's only `other` demonstrations and adding them would erase the only evidence that a lexicon gap resolves correctly — two real backlog lines stay unclassified on purpose.
- 1 Aug 2026 — The answer key is hand-written from the contract, not generated — a script that produces expected values is the resolver, and a key it writes agrees with its mistakes by construction — 80 cases by hand rather than 200 by machine, with the 61 real lines kept as a regression corpus rather than a key.

- 1 Aug 2026 — `verb_lexicon` goes to 52 entries so all 18 members are reachable, and `file`, `send` and `reply` route to their own members rather than to `submit` and `message` — three members existed that no typed word could produce, because a `str.replace` on the lexicon block silently matched nothing and I reported the count I intended rather than the count in the file — `file form 8` moves from `submit` to `file`, and the example changes with it.
- 1 Aug 2026 — `gate2.py` gains a config-liveness check: every vocabulary member reachable from the lexicon, every member carrying its `verb_to_type` and `duration_defaults` entries, no orphans either way — nothing in this project checked a config file, which is how a dead table survived the session that was about that table.
- 1 Aug 2026 — `selftest.py` runs the gate against six deliberately broken copies before the gate is trusted — a gate that passes on real defects is worse than no gate, because it stops the reading that would have caught them — the self-test is now a thing that can itself be wrong, and it caught its own broken setup twice while being written.
- 1 Aug 2026 — `spec.md` states every version once, in a VERSIONS block, and a version number written anywhere else in the file is a defect the gate reports — three stale claims survived four rounds of being pointed out, because each lived in a sentence phrased differently from the ones the gate knew — prose can no longer carry a version, which makes some sentences read more awkwardly.
- 1 Aug 2026 — The example gains a table of the fourteen tasks it contains, typed form and rendered title — eight backlog titles were drawn on screens with no stated origin, and without the table `card_title` cannot be checked at all — no title may be drawn unless it is in the table.
- 1 Aug 2026 — **Correction, and Principle 1's failure.** Session 25 enforced "versions live only in the VERSIONS block" with a blind regex over the whole of `spec.md`, including this append-only log. Six historical entries were rewritten and three became false: `UUID v7` became `UUID the current version`, `v1.0 PDF` became `the current version.0 PDF`, and `a.1 ships 7 action_verbs` became a claim about the current config, which ships 18. Two entries were restored verbatim from an intact copy at `c7/spec.md`; four written after that copy were repaired by hand and are marked. The log is now protected: `gate2.py` refuses any edit to a dated entry, and the version rule applies only above the log.

- 1 Aug 2026 — The decision log is protected by `log.manifest`, one hash per dated entry — the correction entry above claimed the gate refused edits to the log when nothing did, so the claim is now true rather than aspirational — appending an entry means regenerating the manifest, which is a step that can be skipped.
- 1 Aug 2026 — `file` and `reply` badges replace `submit` and `message` on six drawn lines, and two prose paragraphs naming the `a.1` routing are rewritten — moving members changed what the example renders and only the task table was updated — the gate now compares every drawn badge against the task table's verb, which is what found all six.
- 1 Aug 2026 — Badge templates are built from `action_verbs` rather than `\w+` — `banana · 30m` was well-formed and passed — a badge with a real but wrong member still needs the task-table comparison, since shape alone cannot see it.
- 1 Aug 2026 — The stray-version scan exempts only the dated log lines, not everything below the first one — slicing from the first dated entry to end of file left `## DELIBERATE DEVIATIONS FROM PROTOCOL` and every section after it unchecked, and `contract version 99` appended there passed the gate — the exemption is now a line filter, so a version number is caught wherever it is written except inside a log entry.
- 1 Aug 2026 — `log.manifest` protects only the entries present when it was last sealed, and the gate now says so — `zip` stops at the shorter list, so appended entries were silently unprotected until someone remembered to regenerate — the gate warns and `gate2.py --seal` regenerates; sealing stays a separate act because auto-sealing would absorb a rewritten entry that arrived in the same session as an append, which is exactly what Session 25 did.
- 1 Aug 2026 — `example.md` loses a duplicated sentence and the `a.2` reference inside it — the sentence was doubled by an edit and the version number was a second file quietly carrying a version the VERSIONS rule does not govern — the member count is now stated without a config version, so `example.md` has nothing to go stale.
- 1 Aug 2026 — `contract.md` and `types.ts` stop stating which example version they accompany — both still said v23 while the example was at 26, and neither was checked by anything — the gate now rejects a companion version in any file, so VERSIONS in `spec.md` is the only place a version is written.
- 1 Aug 2026 — Every drawn panel in the example must be rectangular, checked by `gate2.py` — substituting `file` for `submit` and `reply` for `message` shortened eight lines by two characters each and left five of seven panels with ragged right edges, and Gate 1 was re-signed over them — Gate 6 is character-for-character, so a panel the shell cannot reproduce is a defect at Stage 1, not Stage 6.
- 1 Aug 2026 — Every card badge ends at the same interior column, and `shell/check_render.mjs` proves it by rebuilding section 1 from the placeholder — `check sensor`'s badge sat one column right of the other three, which no rule produced and which Gate 6 would have failed on — the Stage 3 renderer now reproduces section 1 exactly, so the character-for-character check exists three stages before the gate that needs it.

- 4 Aug 2026 — D-21 closed: the stack is plain ES modules in the browser, no framework, no bundler, Python for the gates — the shell needs three files and a page, and every dependency added now is one the engine inherits before a single rule is written — anything the browser cannot do natively becomes a decision to take later rather than a default taken now.
- 4 Aug 2026 — `resolve()` returns one object with three keys, `task`, `list` and `capture`, and the contract names that shape — Session 30 found the wrapper had no contract item, and Stage 4 forced it, since the key targets `Task` fields and two rendered views cannot answer for them — the shape is neither saved nor shown, so it sits outside the five groups and `gate2.py` checks it separately.
- 4 Aug 2026 — The Stage 4 runner reads every expected value out of `answer_key.md` at run time and holds none of its own — a runner with the answers copied into it is a second key, and the two agree only until one of them is edited — the key's tables have to be machine-readable, which 32 of its cases are not, and the runner reports those rather than guessing at them.
- 4 Aug 2026 — The runner asserts four contract invariants on every case, `raw_text` against the typed line and `id`, `created_at` and `config_version` against what was handed in — the placeholder holds one real hand-copied record, so two key cases matched it field for field and would have passed with no logic behind them, which at Stage 5 is indistinguishable from a real pass — an invariant failure is reported separately from a key failure so the two are never confused.
- 4 Aug 2026 — The four cases the key marks open are parked, not derived, and the runner reads the list from the key's own closing table — deriving them means changing the contract, which is Stage 2 work reopened at Stage 4, and a parked case has no definite value to fail against — Gate 4 cannot be signed while they are parked, so the four are the gate's blocking list.
- 4 Aug 2026 — The contract stops enumerating vocabulary members; `config.ts` lists them and nothing else does — the config table still showed seven `action_verbs` under a column headed with a config version, which is one concept with two spellings and had been stale since the lexicon grew — `gate2.py` now fails if that table enumerates members disagreeing with config, and `selftest.py` plants the stale list to prove it.
- 4 Aug 2026 — `log.manifest` is checked by membership rather than by position — an entry appended to the decision log sits above the dated lines in DELIBERATE DEVIATIONS, which shifted every hash after it and made a correct append read as six edits — a reordering of the log is no longer visible to the gate, which is a smaller hole than an append that cannot be made.

- 4 Aug 2026 — Every answer-key case is a table row; no case states its expectation in prose — the runner reads the key at run time and could not see sixteen cases written as sentences, which meant Gate 4 covered them only by eye — the key is wider and reads less like an argument, and A23's row carries thirteen columns.
- 4 Aug 2026 — In the key, a blank cell means the case does not state that field and an italic marker means it states it as empty — one table now carries verb columns and date columns together, and treating blank as empty would have made A24 claim a date it never mentioned — the two look alike on the page and only the runner tells them apart.
- 4 Aug 2026 — A case the engine cannot be asked about carries a `Handled by` column naming the part that answers it, and F3 leaves `resolve()`'s scope for Gate 6 — a screen limit sitting in an engine key is a case that can never pass, which is indistinguishable from a case that is failing — the key now holds three rows that Gate 4 will never run, and they are visible rather than quietly excluded.
- 4 Aug 2026 — A21 and G5 are marked disputed in place rather than corrected — both expect `other` while the lexicon gives `file` and `bill`, and choosing either side silently is how a key comes to agree with the thing it is meant to test — Gate 4 cannot be signed until both are settled, which is the point of finding them at Stage 4 rather than Stage 5.

- 4 Aug 2026 — `existing_tasks` is a tenth input and carries every open task whole, not a reduced key — the duplicate rule reads each record's stored `normalised`, and handing in a derived summary would mean deriving it somewhere with no contract item behind it — every call site now carries the list, and Part D will have to decide what happens when it is large.
- 4 Aug 2026 — A line below `limits.raw_text_min_chars`, or carrying no letter or digit, never becomes a capture: the button does nothing, the screen shows nothing, and `resolve()` throws if it is called anyway — reaching the engine with a line the screen should not have registered is a screen defect, and a silent empty record hides it — `....` moves from an accepted task to nothing at all, and the minimum itself is a guess at 2.
- 4 Aug 2026 — A comma list sums only when every item is a single token — `sudhi` is one token and `the new CFO` is three, which separates a list from an appositive without a parser — fitted to five pairs, so a two-word list item like `coolindia bill, sudhi` will not sum and nobody will notice until it happens.
- 4 Aug 2026 — The lexicon matches one token at a time, in typed order, first match winning, with no grammar and no word order — that is what makes the rule language-independent, which is the only honest answer for transliterated input — a sentence whose meaning depends on order is read the same as one that does not, and `Ghar kharch hisab` gets no verb at all.
- 4 Aug 2026 — The key was wrong on A21 and G5, not the lexicon — `itr` and `invoice` are entries that earn their place, and the two cases were written before the lexicon grew — `personal ITR` now carries a verb, a type, a context and 30 minutes where it had none, and the `other` rate in section G drops by one.
- 4 Aug 2026 — `resolve()` returns `working` as a third key and `duplicate_dialog` moves from a bare type alias into `CaptureView` — the key asks for `similarity` and for whether a dialog appeared, and neither had anywhere to be returned from — the whole working-values object is now part of every call's output, which is more surface than the screen needs and exactly what a pinned case needs.
- 4 Aug 2026 — Gate 4 passes only when every case it runs disagrees with the placeholder on a value the key itself states — the placeholder returns a fixed id and a fixed config version, so a contract invariant differs on every case and "everything failed" was true before the key was read — five cases were failing on invariants alone and would have gone green at Stage 5 having never been checked.
- 4 Aug 2026 — A case that cannot disagree with the placeholder names its gate in `Handled by` and is not run, rather than being exempted inside the runner — the runner already had that mechanism for F3 and the reason belongs in the key where it can be argued with — E1 restates the Stage 1 example, so the count of cases actually run drops by one and the key holds a row that no longer tests anything.
- 4 Aug 2026 — Section B states `title` and section D states `compare_key` and `numeric_variant` — B8 shares its date part with the example and D9, D10 and D11 stated only that no dialog appears, which an implementation that never shows a dialog satisfies for free — `title` on the marker rows comes out as `call kushan by`, which is ugly and derivable, and B15 states no title at all because whether `at` belongs to the date span is unsettled.
- 4 Aug 2026 — D2, D4 and D7 re-derived at 0.82, 1.00 and 0.42, and D11 now states 0.25 where it stated nothing — the eleven scores were computed before `normalised` was defined as stripping structured spans, and a `—` in D11 read as not applicable when the score is reached — no dialog verdict moves, and D2 now clears `threshold` by 0.02, which makes it the row a real misfire will land on first.
- 4 Aug 2026 — The parked list is read from list items in the key's closing section rather than from the whole block — writing the sentence "F3 and E1 are not run" into that section parked both of them, silently, in the same session that tightened the gate — a case can still be parked, but only by writing a line that looks like a decision, and the key now says so at that heading.
- 4 Aug 2026 — `spec/example.md` section 3 re-stamped to the config actually in force, and `shell/resolve.js` follows because it copies that table by hand — the example named a config version that no longer existed and the placeholder copied it, so `check_render.mjs` compared stale against stale and nothing could catch the drift — Gate 1 has to be pressed again on a finished example, which is the cost of a field whose value is a version number.
- 4 Aug 2026 — The `title` line in `spec/example.md` section 3 no longer claims markers and hedges stay for readability — the example carries no line with a marker and a date together, so it evidenced that claim nowhere and the contract inherited it unchecked — Stage 1 now states only what its own cases show, and the marker rule moves to the contract where the answer key can argue with it.
- 4 Aug 2026 — A marker leaves `title` with the date it governs, and stays when it governs nothing — `by friday` resolves to an instant the card already prints, so the word has nothing left to say and stranded a preposition on every card that carried one, while `after audit` resolved nothing and `deadline` sets an `is_hard` that only reaches the screen when it decided a row's position — five answer-key rows change, strong markers are now treated differently from weak and start ones inside one field, and `raw_text` is the only place the typed sentence survives intact.
- 4 Aug 2026 — `at` belongs to the temporal expression, not outside it — it is in no marker list, so the engine never reads it and a word the engine did not consume cannot be subtracted for any stated reason, which is the same reasoning that puts `this` inside `this friday` — B15 now states a `title` where it stated none, and any future anchor word added to `marker_words` moves from the span to the marker rule with it.
- 4 Aug 2026 — `selftest.py`'s stale-version fixture is repointed at each contract bump rather than rewritten to be version-free — it names the exact line it corrupts, and every version-free alternative tests something weaker than "the gate reads VERSIONS" — a bump breaks the fixture every time, loudly, reported as `SETUP BROKEN` and counted as a miss, which is the failure mode worth having.
- 4 Aug 2026 — `date_firmness` `soft` is written for Part B and has no Part A rendered output — a hedge exists so a date can roll forward quietly rather than be chased, and rolling forward is Part B's, so an output in Part A would be a screen element with nothing behind it — the field is captured and inert for the whole of Part A, which is the same shape as `is_hard` on a dateless task and has to be re-read when Part B opens.
- 4 Aug 2026 — The sentence was added to `schema/contract.md` at its current version rather than bumping it — Gate 2 has never been pressed on this version, so nothing was signed at it and no signature is invalidated — a version number in VERSIONS now covers two states of the file across today, and only the signed ones are pinned.
- 4 Aug 2026 — A strong marker always leaves `title`; a weak, start or point marker leaves only with the date it governs — `deadline` sets `date_firmness` and contributes no content, and it means nothing at all without a date, which is why a dateless one routes to Ideas, while `after` in `after audit` is still relating the words around it — `GST filing deadline` draws as `GST filing`, and one field now treats the four marker groups in two ways.
- 4 Aug 2026 — `at` is a `point` marker rather than part of the temporal expression, and `this` stays inside the span — `date_anchor` has five members and `point` was reachable with no word in any list producing it, while `this` changes no anchor and only picks which Friday — config moves and `date_phrase` on `at 5pm` is `5pm`, not `at 5pm`.
- 4 Aug 2026 — The eight backlog rows in the example were captured on earlier days against their own `now`, and the example now says so — four of them carried due dates their typed text cannot produce at this session's `now`, `file form 8` being overdue chief among them, and nothing stated where those dates came from — the claim is now checkable, and a backlog row with a due date must show the text that produced it.
- 4 Aug 2026 — The backlog row now reads `Social alpha application deadline today` — it was drawn top of the Default list with a due date while carrying no temporal expression, which the contract and the answer key both say is impossible: a strong marker with no date resolves nothing and routes to Ideas — the tier 1 `is_hard` demonstration survives, and `A23` keeps the dateless line as the case that proves the other half.
- 4 Aug 2026 — `selftest.py` fixtures anchor on a heading rather than on a neighbouring line where they can — the decision-log fixture named the entry below the heading and broke the moment one was prepended — a fixture that names a version, like the stale-version one, still has to be repointed at every bump, and both fail loudly rather than passing.
- 4 Aug 2026 — **Correction to the two entries above, made within the session.** Every backlog row in the example carrying a due date is chip-dated, on an earlier day, and no typed text changed — the first fix gave one row a date word and left five others in the same state, and the paragraph written to cover them claimed a dated backlog row shows the text that produced it, which five rows falsified — the answer was already in the example at 5a: a chip sets a `due_at` and subtracts nothing from `title`, so the `Origin` column names the chip and `Social alpha application deadline` keeps its line.
- 4 Aug 2026 — The example is amended at its current version rather than bumped again — Gate 1 has never been pressed on this version, so nothing was signed at it, the same call already made for the contract earlier today — two files now have a version number covering more than one state of themselves, and only the signed ones are pinned.
- 4 Aug 2026 — `gate2.py` reads `shell/config.js` and fails when it is not what `config.ts` emits, comparing every string literal in both — the emitted file sat a version behind for a whole session while `gate4.mjs` imported it, so every one of the 86 cases ran against a config that was not the one in force, and nothing in the toolchain looked — `selftest.py` carries the shell file into its sandbox now, and the check is only as good as tsc keeping literals, which it does because it strips types and nothing else.
- 4 Aug 2026 — A version number written into any companion file is a Gate 2 failure, not a convention — `config.ts` carried `config a.2` in its header and an origin note pointing at an example nineteen versions old, and both were found by eye rather than by a gate — the existing check only caught the phrase "Companion to", which is why it saw neither.
- 4 Aug 2026 — The config export is `partAConfig`, and `gate2.py` fails on a version written inside an identifier — `configA1` outlived a.1 by two versions while `gate4.mjs` imported it under that name, which is the same defect as the header comment and harder to see because a name reads as a name — the rule now covers `config.ts` and `types.ts`, and it is a pattern match, so a version hidden in a name it does not match still gets through.
- 4 Aug 2026 — The identifier check reads every source file in the bundle rather than `config.ts` and `types.ts` — the name it was written for lived in `gate4.mjs` and `shell/app.js`, one directory outside its own scope, which is the same shape as the defect it was catching — it reads code only, with strings and comments stripped, because the decision log has to be able to record what a thing used to be called and `selftest.py` has to hold the broken spelling to plant it.
- 4 Aug 2026 — The identifier check reads its nouns out of VERSIONS rather than matching the word `config` — a versioned thing is exactly a row in that block whose value is a version number, so `example`, `contract`, `config` and `answer_key` are the nouns and the gate rows exclude themselves by carrying a signature instead, which is why `gate4` stays a legal name — an identifier is now read as its parts, split on underscores and case changes, so `key` is a part of `keyA1` and not of `monkey2`, and `config_version` stays legal because `version` carries no digits.
- 4 Aug 2026 — Four aliases are hand-written into the check, `schema`, `key`, `types` and `cfg` — the nouns in VERSIONS are the names of the files, and the code calls three of those four something else, so reading VERSIONS alone would have missed the two names this session was opened to catch — the alias list is the part of the check that can go stale, and it is now the only part, recorded in FOUND, NOT FIXED.
- 4 Aug 2026 — A gate that exits non-zero on a traceback is counted as a miss, not a catch — the fixture that renamed `partAConfig` broke the regex that finds the config body, so the gate died before the identifier check ran and had been proving nothing for two sessions while reporting `caught` — the fixture now plants a versioned name beside the export instead of on it, and two more plant names with no `config` in them at all.

- 4 Aug 2026 — `schema` stays in the alias list although no identifier in the bundle holds it — the four versioned things are named by files under `schema/`, so the word is what a name would reach for the day one is written, and an alias that covers nothing costs a line while a missing one costs a stale name nobody sees — the list now mixes two kinds, three names in use and one held in reserve, and only a reading tells them apart.
- 4 Aug 2026 — Gate 2 was pressed by hand on the widened check: a versioned name planted in a throwaway source file failed the gate naming the file and the name, and the gate returned to passing when it was deleted — the press is watching it catch something, not reading that it passed — `KEY` in `gate4.mjs` is now the live name nearest the all-caps hole, which moves that entry in FOUND, NOT FIXED from theoretical to adjacent.

- 4 Aug 2026 — Rule 1 is the verb and the three fields config derives from it, `commitment_type`, `context` and `est_duration_min`, rather than the verb alone — those three read one map each with the verb as the key and no other input, so writing the verb without them would leave three fields that cannot be wrong on their own and no case able to turn green — the rule covers five fields, and a later rule that overrides any of them, the type chip most of all, has to say so.
- 4 Aug 2026 — `resolve()` now returns `raw_text`, `id`, `created_at` and `config_version` from the input rather than from the hand-copied record — these are handed in and never derived, and while they stayed fixed no case could be green no matter which rule was written, because the runner compares all four on every case — the record identifies itself from this session onward, and the placeholder is no longer a single fixed answer.
- 4 Aug 2026 — `gate4.mjs` reads the current stage out of `spec.md` and inverts its pass condition on it — the same command means opposite things either side of Stage 5, and a flag would be a second place to forget, so the stage line becomes the single source — the stage line is load-bearing now: written wrong, the runner reports a wrong verdict instead of failing, and that is recorded in FOUND, NOT FIXED.
- 4 Aug 2026 — A2 and A26 are left red rather than parked or corrected — the key says `check` gives the `phone` context and `verb_to_context` has no entry for `check`, so one of the two is wrong, and parking them would hide the disagreement behind a skip while correcting either side silently is how a key comes to agree with the thing it tests — they stay in the failing count until the next session settles which file changes.

- 4 Aug 2026 — `check_render.mjs` and `check_loud.mjs` hand `resolve()` a real config instead of an empty object — both were written against a placeholder that ignored its input, so the first rule that read one crashed them, and a caller that omits a contract input is the caller's defect rather than a reason for the engine to default — the two Stage 3 harnesses now build a full input, and any later rule that reads a tenth input will break them the same way, loudly.

- 4 Aug 2026 — The key was wrong about `check`, not `config.ts`: A2 and A26 read `undetermined` — `phone` means the work happens on a phone and checking a sensor is the sensor, `verb_to_context` holds only the verbs that imply a context, and `confirm` and `give` already read `undetermined` in the same table on the same reasoning — the key bumps, Gate 4's signature stays pinned to the version it was pressed on, and the two rows turn green with no engine change.

- 4 Aug 2026 — The Stage 3 placeholder is kept as `shell/resolve.stage3.js` and `gate4.mjs --placeholder` runs the current key against it under the Stage 4 reading — the previous session recorded that a key edit could no longer be checked the Stage 4 way, which was wrong: nothing was lost except the file, and the file is one copy — the entry in FOUND, NOT FIXED is withdrawn, the frozen file is never edited again, and a later change to `resolve()`'s return shape has to be mirrored into it or the runner fails against it loudly.

- 4 Aug 2026 — The key states three cell readings: a value, `*(empty)*`, and `—` for a field the key does not speak to. A blank fails the run — a blank was skipped, so a column a table declared could sit unstated and the engine could return anything into it, which was 61 fields across twelve rows — every blank is filled now, and a row added without filling its columns fails rather than passing quietly.
- 4 Aug 2026 — Section B carries `date_marker` and `date_phrase` as columns, and every section producing a record carries `list` — the prose asserted the first two and nothing checked them, and no row anywhere stated the routing the user actually sees — the two B rows that were indistinguishable now differ, and the cases that turned red are the key testing something rather than a regression.
- 4 Aug 2026 — `similarity` is rounded half-up to two decimals before it meets `threshold`, and the contract says so — the scores were checkable only to the digit an unstated rounding rule produced, and one of them, 0.4468, reads 0.44 truncated and 0.45 rounded — the contract's padding was already asymmetric and already correct, and every score in the key reproduces from it, so nothing moved except the rule being written down.
- 4 Aug 2026 — An `end` anchor resolves to 23:59:59, one second below the exclusive bound, and instants are stored to the second — the key wrote 23:59, a third way of writing the same edge that loses the last minute of the day — two rows change and the rule is stated once in the contract rather than implied by two cells.
- 4 Aug 2026 — B8 clips its `earliest_start` as B1 does — one rule was behaving two ways on two rows, clipping the midpoint on both and the start on one — the clipped start is what an engine will produce, so the row agrees with the rule rather than with the version of it written first.
- 4 Aug 2026 — Two rows enter section C to separate the comma rule from its rivals — *every item is a single token* fitted five rows exactly as well as *no determiner* and *three items or fewer* did, so the rule could not be falsified by anything in the key — a two-item list with a two-token item and a four-item list of single tokens disagree with the other two whichever is right.
- 4 Aug 2026 — Twelve gap cases are written and three are parked with the question stated — a gap named in an audit and not written stays open, and a case written with a guessed value is worse than no case — a typed duration has no `duration_source` member, a day and a time in one line has no precedence rule, and an ended band has no arm, and the runner skips all three so none can pass by accident.
- 4 Aug 2026 — Two cases carry their own `now` and `gate4.mjs` reads a per-case anchor — every case shared one clock, so an engine reading the wall clock instead of the handed-in `now` would have passed the whole key — Sunday 23:30 is the second anchor and the only thing here that catches that class of defect.
- 4 Aug 2026 — The companion-version check catches a config version in prose in any companion file rather than in two spellings — the key's own header carried one for three sessions in a third spelling — a version inside a table cell is an illustrative Example value and stays unchecked, and `spec/example.md` is exempt while the deviation licensing its older stamp stands, both recorded in FOUND, NOT FIXED.

- 4 Aug 2026 — A duration typed into the line is not read, and `duration_source` gains no member — `call kushan 30 min` needs a fifth setting meaning *the user wrote it*, and a vocabulary member can never be removed once it ships, so adding one for a parser that does not exist buys a permanent commitment against a guess — the number stays in `title` and `raw_text`, the case states the current behaviour rather than being parked, and the rule is on the LATER LIST with a row already watching it.
- 4 Aug 2026 — The anchor comes from the marker when there is one and from the expression when there is not, which reverses the reading that `at` produces `point` — `friday 5pm` names one instant and carries no marker, so a word the user did not type cannot be what makes the anchor — `at` stays in `marker_words` for the only job it has left, leaving `title` with its date, and B15's values do not move although the reason for them does.
- 4 Aug 2026 — A time beats a day when both sit in one line — the day supplies the date and the time supplies the instant, and the finer expression is the one the user was more specific about — `friday 5pm` is `time` precision with the day window gone, and `date_phrase` keeps both words because both were one expression.
- 4 Aug 2026 — A band that has already ended rolls to the same band tomorrow — `morning` typed at 14:00 resolved in the past would be overdue the instant it was typed, and nobody types a time of day that has gone meaning the one that has gone — only bands roll, and a named day in the past still resolves in the past, which two cases already state.

- 4 Aug 2026 — A date word is config and a date number is code: `date_lexicon` gains the seven weekdays and `yesterday`, while a calendar date and a clock time are patterns the engine reads — the line falls where translation falls, because a Gujarati user needs their own word for Friday and needs nothing new for `20` — config bumps and Gate 2 needs pressing again, and a weekday in another language is now one config entry rather than a code change.
- 4 Aug 2026 — A named calendar date in the past stays in the past — the first draft rolled `1 aug` forward a year, which is what a booking form does and the opposite of what a missed deadline needs — only a band rolls, and a date the user spelled out shows overdue, which two cases state.
- 4 Aug 2026 — A weekday that names today resolves to today, not a week ahead — the day window runs to midnight, so there is still a day left to do the thing in — no case in the key covers it and the choice is recorded in FOUND, NOT FIXED rather than evidenced.

- 4 Aug 2026 — `in` makes a duration into a time, and it lives inside the expression rather than in `marker_words` — `call kushan in 30 mins` is an instant and `call kushan 30 min` is a length, and one word is the whole of the difference — `in` is treated like `this` in `this friday`, changing no anchor and leaving `title` with the words it governs, so no config member is added and the reading of A27 stands.
- 4 Aug 2026 — A band and a clock time in one line contradict each other and the time wins — 14:00 is not the morning, and the alternative is resolving to a window the user's own second word rules out — both words stay in `date_phrase` so both leave `title` together, and this is the same precedence a day and a time already follow.
- 4 Aug 2026 — A rolled band offers the day it came from as a chip, and does not stop to ask — the roll is a guess and the user should see it and reverse it in one tap, while a dialog that blocks the capture is the one thing D-1 forbids — the chip is a Gate 6 obligation and the record is dated either way, so nothing waits on the screen being built.

- 4 Aug 2026 — Every date in the key carries its weekday and `gate4.mjs` checks the weekday against the date — a reader cannot verify `7 Aug 16:30` by hand without a calendar, which makes a key that exists to be read by hand unreadable, and a stated weekday is a second assertion the same row has to satisfy — a row that says Friday about a Thursday now stops the run, which was proved by planting one.
- 4 Aug 2026 — A case carrying its own clock states it in the second column, not the last — three rows override `now`, and all three read as errors to anyone who reaches the values before the clock: `next month` is September at the shared anchor and August at a July one, and both are right — the column that changes how every other cell is read now sits where it is read first.
- 4 Aug 2026 — Section H puts a verb and a date in one line, six cases — section A is 27 lines with no date and section B is one body with 31 dates around it, so nothing tested the two halves of the engine together and nothing put a task on the Default list that also had a verb, a type, a context and a duration — a verb rule that read a date word as a verb, or a date rule that ate the noun beside it, passed the whole key before this and fails here.
- 4 Aug 2026 — E1 states `Default` rather than a dash — the row is handed to Gate 1 and never runs, but it states every other value it has, so a dash in one column read as an omission rather than a decision.

- 4 Aug 2026 — Gate 4 is signed by hand on the current key, and `selftest.py` gains a fixture read by `gate4.mjs` rather than `gate2.py` — the weekday check was proved by planting a bad row once, by hand, which is exactly the kind of proof that stops being true the session after it is made — the self-test now carries the shell files the key runner imports, and one of its cases exercises a different gate, which is a shape the file did not have before.

- 4 Aug 2026 — Gate 2 is signed by hand on the current contract, four contract versions and one config bump on from the last press — the script had passed on every one of those bumps, and a script passing is what Gate 2 checks rather than what it is — the fourteen rules in `Rules the answer key forced` were read back in plain words first, and the signature covers the wording as much as the counts.

- 4 Aug 2026 — H6 stated a title keeping its strong marker, which contradicts A23, E3 and the contract rule all three rest on — the row was written three sessions ago and read past twice, including at the Gate 4 press, and it was found only when the rule that reads it was being written — the key bumps, and it is the first case where writing a rule caught the key rather than the other way round.
- 4 Aug 2026 — `title` drops the words the engine consumed and touches nothing else — a label that is not the typed line has to be derivable from it, and the alternative, rebuilding a title from parsed pieces, loses the user's own casing, punctuation and spacing — `call    kushan` keeps its four spaces, `call kushan!!!` keeps its marks, and `raw_text` still holds every character in every case.

- 4 Aug 2026 — A hedge leaves `title`, reversing the rule that it stayed there and stripped only from `normalised` — the title rule takes out the words the engine consumed and a hedge is consumed like any other, so keeping it made `title` and `normalised` differ for a reason no other pair of fields differed for — five rows change, `date_hedge` and `raw_text` still hold the word, and a screen trace for `soft` is now owed and recorded as owed.

- 4 Aug 2026 — `due_phrase` carries the hedge: a soft date reads `Tuesday, maybe` — the hedge stopped reaching the screen when it left `title`, and it belongs with the date rather than as a mark of its own or a clause in `card_reason`, which explains why a card sits where it sits and would then be explaining something else — rendering a hedge is not acting on one, so rolling a soft date forward stays Part B's, and the obligation is unchecked until `due_phrase` becomes a rule with a column in the key.
- 4 Aug 2026 — `list_header` is `Default` when `due_at` is set and `Ideas` when it is not, and nothing else decides it — not the verb, not the type, not whether a marker was typed — `check_render.mjs` now hands `resolve()` the line the example was captured from, because an empty line routes to Ideas and section 1 draws the Default list.

- 4 Aug 2026 — `normalised` is derived from `title` rather than stripped out of `raw_text` a second time — the contract defines both as the line minus the same structured spans, so two separate strippers would have to agree forever and would eventually stop agreeing — the two fields cannot now disagree about what a span is, and the cost is that a future rule wanting them to differ has to say so explicitly rather than by editing one stripper.
- 4 Aug 2026 — A token with a digit inside letters is not a number: `compare_key` drops `8` and keeps `gstr1` — the rule exists so `file form 8` and `file form 9` read as the same words, and a name that happens to carry a digit is still a name — this is what makes C2 fall to `other` and what stops a version-numbered task merging with its neighbour.

- 4 Aug 2026 — The duplicate check runs against every open task and the highest score is the one recorded — `existing_tasks` is a list and the contract never said which of several a single `similarity` refers to, and the best match is the only one a dialog could sensibly name — a capture that half-matches four old tasks now reports one number, and which task the dialog names when two tie is still a Gate 6 question.
- 4 Aug 2026 — `gate4.mjs` compares a numeric field as a number rather than as text — the key writes `1.00` for a ratio and a ratio in the record is the number 1, so five cases failed on a trailing zero while agreeing on the value — the runner reads both sides as numbers when both are numbers, which also makes `5` and `5.0` the same for a duration, and text fields are untouched.

- 4 Aug 2026 — The commas count the items and the first chunk is always one, replacing the rule that counted the words in every item — under the old reading something had to throw away `Payments` and `to` before `coolindia` could be a single-word item, and nothing said what, which would have meant inventing a category of glue word the contract has never needed — C6 flips from 10 to 20, and C8 enters as its mirror: the same two chunks the other way round, which sums one way and not the other.
- 4 Aug 2026 — Two rules were written in one session, the comma sum and the refusal — the refusal is four lines and shares nothing with anything else, and splitting it would have cost a seal and a session for a guard clause — the protocol says one job per session and this was two, recorded rather than hidden.

- 4 Aug 2026 — A date chip appends its label to the text box and the engine has no chip input, taking `CaptureInput` from ten fields to nine — a chip was a second way for a date to reach the record, which meant two paths to keep in step forever and a rule about which wins when both fire, and there may be many chips — a new chip is now a screen change rather than an engine change, `Pick date` and `Park` produce no words and are deferred, and `spec/example.md` contradicts all of this until it is rewritten.

- 4 Aug 2026 — The example's chip-dated rows become typed rows carrying their date words, rather than being given chip labels — after the chip decision a chipped row and a typed row are identical in the record, so an `Origin: date chip` column records a distinction nothing can tell apart, and inventing six chip labels would have written a second story into Stage 1 that Stage 2 cannot read — every drawn title is unchanged, because the title rule takes the date words out again, and two rows now state the earlier day they were typed on, which is what makes `friday` mean 31 July and `file form 8` overdue.
- 4 Aug 2026 — `chip_presets` stays in config and is named there as screen vocabulary the engine never reads — the screen needs the list at Stage 6, and moving it out of config would be a bump that buys nothing today — the cost is one object in a config whose other objects are all read by the engine, and the contract row now says which kind it is.

- 4 Aug 2026 — Every row in the task table states the moment it was typed, and the example says once that everything drawn is seen at 10:40 — a window is cut at whatever `now` was when the line was typed, so `check sensor today` typed at 09:30 is due 16:45 and the same words at 10:40 are due 17:20, and stating only the day left four rows underivable — the table gains a column, the example is amended at its current version because nothing was signed at it, and Gate 1 is pressed on the result.
- 4 Aug 2026 — `gate2.py` reads the example's task table by column name rather than by position — adding one column broke a reader that counted pipes, and it broke loudly, which is the only reason it was not shipped — two other tables in that file are still read positionally and will break the same way.

- 4 Aug 2026 — `deadline_band` reads `today` as the same calendar day and `this_week` as running to the end of Sunday, so on a Monday a task due the following Monday is `later` — a rolling seven days would make the band depend on the hour as well as the day, and a week that ends on a named night is the one a person can check against a calendar — the band changes at midnight on Sunday for everyone at once, which is what a shared word has to do.
- 4 Aug 2026 — A soft date reads `Due around Friday` — the hedge shows where the date shows, and `around` is a word people already use for exactly this — it applies only where the phrase names a point, because `Due around today` and `Due around this morning` say less than the plain forms, and a hedge on those now lives only in `date_hedge`.
- 4 Aug 2026 — A stated time no longer outranks the day: `friday 5pm` reads `Due Friday at 5pm` — the first version of the rule put a time above everything, so a task four days out read `Due at 5pm` and lost the four days — a stated time is the finest thing said about the hour, not about the date, and `tonight` takes no `this` because it already carries its day.
- 4 Aug 2026 — Both of those were found by writing the key column before the engine agreed with it — 37 rows of `due_phrase` were derived by hand from the stated table and two of them came out wrong, which is what a key is for — the code was changed and the key was not, which is the direction that has to hold for the rest of this build.

- 4 Aug 2026 — A span wider than a day says the span: `Due next month`, `Due next week`, `Due this weekend` — the instant exists so the task can be ranked, and printing it read `Due 16 Sep` for a line whose owner said `next month`, which claims a precision they never gave — five key rows change, and the rule is the same one already written read from the other end: the finest granularity that is true, not the finest available.

- 4 Aug 2026 — The commas are counted on `title` rather than on the typed line — `call kushan, tomorrow` read as two calls because `tomorrow` sat after a comma, and a date is never a thing to do — a separator left with nothing to separate now goes with the words it was separating, so the card does not read `call kushan,` either.
- 4 Aug 2026 — `on` joins `this` as a word that belongs to the expression rather than to `marker_words` — `on Thursday` left `on` behind and the card read `Meet Priya, the new CFO, on`, and a marker group for a word that changes no anchor would have been a group with nothing in it but this — both words are still written in code rather than in config, which is the one place a date word is not config, recorded here rather than fixed.
- 4 Aug 2026 — The order-dependence stays: `pay coolindia invoice, sudhi` sums and `pay sudhi, coolindia invoice` does not — the same two payments give different answers depending which is typed first, which is a real oddity kept on purpose, because allowing a chunk after a comma to be more than one word would read `Meet Priya, the new CFO, on Thursday` as three meetings — C6 and C8 state both orders so the oddity is written down rather than discovered.
- 4 Aug 2026 — No ceiling on a comma sum: eight items give eighty minutes — a batch of eight payments is quicker each than eight separate ones, and any cap would be a second fitted number on top of a fitted rule — the LATER LIST already holds batch response by verb, which is where the real answer goes.

- 4 Aug 2026 — `duplicate.threshold` moves from 0.8 to 0.6, and the answer key notices nothing — the key's scores leave a gap between 0.50 and 0.82 with nothing inside it, so every value in that range gives every verdict it states, and the old number was a guess inside a range rather than a measured edge — the new number is the same kind of guess, chosen to interrupt sooner, and both the contract and the key now say the range is unpinned rather than implying the number was tested.

- 4 Aug 2026 — The rules block states its own count and `gate2.py` reads it — the sentence said seven while the block held nineteen, which is the same defect class as every stale count already fixed in this project and it survived because nothing counted the paragraphs — the block also now says where the rules that are not in it live, because a reader sent to one place for something spread across four finds nothing and concludes nothing changed.

- 4 Aug 2026 — `in 30 mins` reads `Due in 30 mins` rather than `Due at 11:10am` — someone thinking in lengths is not thinking in clock time, and converting for them is work the card can do and should not — it holds while the answer stays a length, the same day and inside twelve hours, and `in 20 hours` reads `Due tomorrow at 6:40am` because by then the clock is plainer; B32 is the row on the far side of that edge.

- 4 Aug 2026 — The shell fills the seconds into `now` before calling, rather than the engine accepting an instant without them — instants are stored to the second and an engine that guesses at a missing field is an engine that guesses, but the box a person types into is a caller we own and it was handing a value `resolve()` correctly refused — found by typing into the running shell, which is the first defect Gate 5's reading was never going to catch.

- 4 Aug 2026 — The shell re-resolves on every keystroke and prints the record beside the drawn panel — pressing a button per line made trying twenty phrasings a twenty-press job, and the defects being found now come from trying phrasings rather than from reading rules — a refusal is marked on the error it throws, so the screen can tell `nothing typed yet` from `the engine is broken` and only the second stays loud, which is the distinction Gate 3 was pressed on.

- 4 Aug 2026 — `index.html` loads `app.js` under a fresh query on every load, and `app.js` passes that query to the three modules it imports — `Cache-Control: no-store` covers the document and not the modules it names, so a page that looked new ran yesterday's code twice, and the second time it read as a defect in the live shell rather than as a stale file — the cost is a query string in the shell's imports, and the harnesses are untouched because they import `resolve.js` directly.

- 4 Aug 2026 — The shell prints every field the engine returns, built from the returned object rather than from a list written into the screen — a hand-picked thirteen showed the fields I thought were interesting and hid the other forty-four, and it would have gone stale the first time a field was added — all 37 saved fields, all 13 working values and both shown groups are on screen now, which is what makes typing a line a way of reading the record rather than a way of reading the panel.

- 5 Aug 2026 — The shell marks every field by what it is expected to do, from two lists written into the screen: NO_RULE for a field the contract records and nothing writes, NO_INPUT for one only an input this screen cannot supply would move — both lists are hand-kept and both go stale the moment a rule lands, which is the point: a NO_RULE field that changes is drawn red, so the staleness announces itself rather than hiding.
- 5 Aug 2026 — Every field that changed since the previous line is marked, so one word can be altered and the effect read off — checking one variable across many sentences was the ask, and diffing consecutive lines is the version of it where the reader controls exactly what differs, rather than reading twenty rows and inferring.
- 5 Aug 2026 — The shell prints one sentence saying what it understood — it is composed from the same fields it summarises, so it cannot catch a field being wrong and only makes one easier to notice, and that limit is worth stating because a readback that looks like a check is worse than none.

- 5 Aug 2026 — Four of `resolve()`'s nine inputs were read zero times by any rule, and every frozen field in the capture group traced back to one of them — 114 green never caught it because every case in the key hands in `null` for all four, so the key was testing nine inputs' worth of contract through five of them — Stage 5 reopens: the taps are record rules and belong here, and `bound_task_id` writes no record field so its four strings are Gate 6's.
- 5 Aug 2026 — A tap outranks what the line implied, and `type_source` records which — the person tapped after reading what the line gave them, so the later instruction wins, and a field that says only `deadline` without saying who decided it cannot be told from a lucky parse.
- 5 Aug 2026 — `significance_row` carries the labels and no emphasis marker — the contract asks for the current one emphasised and the example draws no character for it, so emphasis is styling and putting a star in the string would invent something the screen never showed — the field stays constant, correctly, and stays on the no-rule list.
- 5 Aug 2026 — The parsed date chip reads `\u2713 this morning` for a line whose `date_phrase` is `morning`: it is `due_phrase_short` with the leading `due` removed — the example draws the rendered phrase rather than the raw span, and the chip and the card now cannot disagree — the row holds what fits, dropping `Pick date` and `Park` once the parsed chip has taken the space, which is what the example's own panel shows.

- 5 Aug 2026 — `check_render.mjs` compares against the example's typed capture row rather than section 1's panel — section 1 draws an empty box and `resolve()` refuses an empty line, so that panel is not something the engine can produce, and the harness only agreed with it while `chip_row` was a copy of it — what it checks now is real: five lines the engine writes end to end, the parsed chip, the wrap, the type chip and the marked significance button.
- 5 Aug 2026 — The chips wrap rather than drop, and the marked significance button is drawn rather than styled — both were read off the example's own second panel after the first draft guessed the other way on each — `[Pick date][Park]` moves to a second line and `[**High**]` carries its marks, which is what that panel shows and what the contract's `current one emphasised` meant.
- 5 Aug 2026 — The row holding the type chip and the significance buttons is drawn section 2's way, three spaces after the chips and one inside the border — section 1 right-aligns the same row and nothing states a rule for either — the two panels cannot both be matched, section 2 is the one a typed line produces, and section 1's version is recorded as unchecked rather than quietly abandoned.

- 5 Aug 2026 — Section I taps a type, a significance and a bound task, seven cases — every other section hands in `null` for all four taps, so the key tested nine inputs' worth of contract through five of them and four inputs sat read by nothing for eleven sessions — I2 is the only demonstration anywhere that `type_source` can read `user`, and without it a rule that stopped writing the field would pass the whole key.
- 5 Aug 2026 — `is_hard` reads `date_firmness` and the two windows are written out of the date rule — all three were computed and thrown away, and the ranking's first tier reads `is_hard`, so a tier-1 override was dead in a way no case could see — I5 states it now, and `resolved_window` and `clipped_window` are `null` where no window was resolved rather than holding a stale one.
- 5 Aug 2026 — The runner sets every tap to `null` first and spreads a section's own taps over the top — written the other way round the nulls silently won and section I failed on three rows while the engine was right, which is the same defect shape as a hand-picked list overriding a derived one.

- 5 Aug 2026 — Shorthand is a config table, `date_aliases`, expanded before anything is looked up — `tmrw` and `fri` are words, and words differ by person and by language, so a table is where they belong and a Gujarati user adds their own without a code change — the typed word is still what `date_phrase` reports and what leaves the title, so nothing in the record says which spelling was used.
- 5 Aug 2026 — A clock time is read in five shapes rather than two: the minute separator can be a colon, a dot or nothing, and the meridiem can be its own word — `5.30pm` and `5 pm` produced no date at all, which is worse than producing the wrong one, because the line silently became a dateless capture — a bare number with neither separator nor meridiem is still not a time, which is what keeps `file form 8` from acquiring one.
- 5 Aug 2026 — `next` joins `this` and `on` as a word picking which day the expression means — `next friday` was resolving to this Friday and leaving `next` in the title, so the card read `call kushan next` about the wrong day — none of the three changes an anchor, which is what makes them the expression's and not `marker_words`'.
- 5 Aug 2026 — Trailing punctuation is not part of an expression — `pay a tomorrow, b` read its span as `tomorrow,` and took the comma out of the line with it, which merged two payments into one and summed them as one — what the title drops is the words, and punctuation beside them stays where it was.

- 5 Aug 2026 — The first date typed wins and the first marker typed wins, replacing the longest word winning either — reversing the words changed nothing before this, which means the engine was reading no order at all and the behaviour was an accident of how the search was ordered rather than a rule anyone chose — a longer phrase still beats a shorter one starting in the same place, so `next week` beats `week`.
- 5 Aug 2026 — Every date expression and every marker leaves `title`, loser included — a second one is the person correcting themselves, and leaving it behind puts a date on the card that the record does not hold, or strands a preposition beside a date that is not there — `call kushan tomorrow friday` draws as `call kushan` and the record says tomorrow.
- 5 Aug 2026 — A bare clock time that has gone means the next one, and only a bare one — `at 10AM` typed at 10:40 was landing overdue as it was typed, which is the same wrong answer the band rule was written to avoid — `1 aug at 5pm` names a day and a named day is never moved, which is the guard that keeps the rule from swallowing a missed deadline.
- 5 Aug 2026 — `go to office` keeps its five minutes and the lexicon is left alone — `go` is in no list, so an office visit reads as a five-minute other, and the honest fix is a lexicon grown against real captures rather than one verb added because one line was typed — recorded as kept rather than fixed.

- 5 Aug 2026 — A parser built on chrono-node and wink-nlp was run against the whole answer key rather than judged by reading it — it passes its own sixteen cases and agrees with 78 of our 133 once its placeholder vocabulary is replaced with ours, which is the only number that decides anything — the harness is kept outside the repo, because a second engine inside it would be a second thing to keep in step.
- 5 Aug 2026 — The 55 disagreements are ours to keep, not defects in either engine: `1 aug` becomes 2027 under chrono's `forwardDate`, `by friday` lands at 18:29 rather than 23:59:59, `someday` is not a date word to it, a weekend is a week, `after 5pm` is a point rather than a start, and a comma list is never summed — each is a decision this project made and wrote down, and a library that has not made them is not wrong, it is silent.

- 5 Aug 2026 — Word endings are read by rules in the engine and irregular forms sit in `verb_irregulars` in config — an ending is how English spells a word and an irregular form is a word, so the two belong in different places, and the lexicon held 52 tokens for 18 verbs with a third of them endings added by hand after someone typed one — `replied`, `paid`, `booked` and `sent` all fell to `other` before this, and a hand-kept list of endings can never be finished.
- 5 Aug 2026 — The language model the outside parser uses was not needed for this and is not shipped — it was agreed to and then turned out to buy only the words neither the spelling rules nor a twenty-line table reach, at 10 to 15 MB in an app that is one text box — it stays on the LATER LIST as a config-shaped thing rather than a dependency, to be added once it can be measured against the key rather than argued about.
- 5 Aug 2026 — A weak, start or point marker counts only when the date starts right after it — `pay by cheque` and `order caps by courier` both recorded `by` as a date marker, and a later rule reading that field would have read a deadline into paying by cheque — a strong marker is exempt because it needs no date to mean anything, which A23 states and which the first draft of this rule broke.
- 5 Aug 2026 — Adjacency decides between two markers and order no longer can — two of them cannot both touch the date, so B40 and B41 now state the opposite of what they stated at version 23, when order was the rule and adjacency did not exist — order still decides between two dates, where both can sit anywhere in the line.

- 5 Aug 2026 — The language model ships, read last in the verb chain, after the lexicon, the irregulars table and the spelling rules — measured first and it reached nothing the three before it did, on thirty-six inflected lines, and on six of them the rules beat it because it lemmatises `spoke` to `speak` and `speak` is not one of the eighteen verbs — the order is what makes it safe to ship something that changes nothing: last means it can only add, so no answer moved and every case stayed green.
- 5 Aug 2026 — It is bundled once and the bundle is committed, rather than installed — the shell serves from a static file server with no build step and the gate runs in node, and both read the same 3.6 MB file, so there is one artefact and not two that can drift — the command that rebuilds it is written at the top of the file, because a generated file with no recipe beside it is a file nobody can regenerate.
- 5 Aug 2026 — The four lines neither reaches are the argument for what comes next: `handed over`, `acknowledged`, `dispatching` and `hand over` fail because `hand`, `acknowledge` and `dispatch` are in no lexicon — the model gets every ending right and still returns `other`, which says the gap is vocabulary and vocabulary is a config list.

- 5 Aug 2026 — `chip_spans` enters both the input and the record: the character ranges where a chip's words sit, handed in and stored unread — the chip decision said a date arrives one way and that still holds, because the engine derives nothing from the ranges and returns an identical record without them, which was checked rather than asserted — recording how words got there is not a second way for them to arrive, and treating the two as the same thing is what left this out until now.
- 5 Aug 2026 — The screen keeps the ranges as the line is edited, not the engine — only the screen saw the edits, and the whole value of the field is in what it says after one: someone taps `This afternoon`, reads what it gave them and changes the words, which is the strongest signal a chip said the wrong thing and cannot be read from a line that no longer remembers which words were tapped — a range that no longer covers what was tapped is the answer, not a fault in it.
- 5 Aug 2026 — `chip_spans` is stated on every row of section E, not only on the one with a chip — a field written where nothing was tapped is the defect this field can have, and only a row expecting an empty list can catch it.
- 5 Aug 2026 — `gate4.mjs` compares a list as a list — `String([])` is the empty string and `Number([])` is zero, so an empty list read first as "the engine returned nothing" and then as the number zero, and both readings said the row agreed — every list-valued field in the contract was uncheckable until this, which is four of them.

- 5 Aug 2026 — A task carrying any resolved date is in Default, `due_at` or `earliest_start`, rather than only one carrying a due date — `after friday` is a task with a day attached and a person who typed one is not filing an idea, while Ideas is for a line carrying no date at all — three rows move, B7, B17 and B40, and they carry no `deadline_band`, so they rank below everything that has one, which is where a task you cannot start until Friday belongs rather than out of the list.
- 5 Aug 2026 — Those three now draw a card with no sentence on it: `due_phrase` reads `due_at` and there is none — `From Friday` is what belongs there and no rule writes it, so it is recorded in FOUND, NOT FIXED rather than invented in the same session that made it visible.

- 5 Aug 2026 — A task with a start and no due date says `From Friday` rather than nothing — `Due` would be a lie on a task with no due date, and the alternative on the table was silence, which is what those cards drew for one session — the forms mirror the due ones exactly so the two sentences cannot drift apart in wording, and a time today says only the time because `From today at 5pm` is longer and says no more.
- 5 Aug 2026 — A start whose time has already gone does not roll, where a due date does — `after 5pm` typed at six means today, because the person is saying when they can begin and they can already begin, so moving it to tomorrow takes away a task they could do now — `at 5pm` at six is still tomorrow, since a due date in the past is a task born late, and B42 and B44 are the same words an hour apart with the two anchors so the exception is readable rather than buried.

- 5 Aug 2026 — A tapped date beats a typed one, and the screen clears the chip when the line is edited afterwards — the engine reads a finished line and cannot know which happened last, so the pair of rules is what "the later one wins" means, and each half sits where the knowledge is: the engine has `chip_spans`, the screen saw the events — without the clearing half, a chip tapped and then typed over would keep winning.
- 5 Aug 2026 — A chip tap replaces the previous chip's words rather than adding to them — two taps were putting two dates in the line and the second doing nothing visible, which is the same defect as a chip tapped after a date was typed — the shell already behaved this way because its chip is a dropdown, so nothing broke and nothing said so, which is how a real screen would have got it wrong by appending.
- 5 Aug 2026 — A losing date expression takes its lead word with it — `this` and `on` and `next` were kept for the winner only, so `friday This afternoon` dropped `afternoon` and stranded `This` in the title — found by E8, the row written to prove the tap rule, on the run that added it.
- 14 Aug 2026 — T14 closed on section 2's layout: chips wrap, then three spaces, the type chip, the fill, the significance buttons, one space inside the border — section 1's right-aligned variant with its `· · ·` leader was the empty state drawn under a second rule, and one panel per state is the point of a finished example — the leader is deleted from both panels, from `render.js` and from `gate2.py`'s template list.
- 14 Aug 2026 — The chip wrap budget reserves the space inside the border — redrawing section 1 under one rule showed the empty screen throwing out of `row()`, because the six presets fit the chip line exactly and left nothing for the tail below — the screen the app opens on had never been rendered by anything, since `resolve()` refuses an empty line and no harness could reach it.
- 14 Aug 2026 — Ranking factor 5 reordered on a principle rather than left as the Stage 2 guess: how much of the task is yours to do right now — `waiting` sat at 9, above three types you also cannot act on but which are at least yours, and `deadline` at 2 was nearly dead because tier 1 takes the hard ones before this factor runs — three members moved, and the order is still fitted to nothing, which is recorded rather than hidden.
- 14 Aug 2026 — `alarm_lead_by_type` goes in with every value the same, so the shape exists and no number is claimed — a correction later becomes a number change rather than a structural one, and nothing is recorded as evidence that is only a guess, which is the opposite of how `type_order` and the two thresholds arrived — while the values are equal the table changes no behaviour, and the contract says so.
- 14 Aug 2026 — One alarm lead for every task, changed per task, rather than a lead suggested from the commitment type — the table would have been fitted to nothing like the others, and a number the person can move is more honest than a guess dressed as a rule — the cost is that the default is short for a long job and the app may not say so, because duration is a quiet field.
- 14 Aug 2026 — An alarm needs a stated time — a task due `Friday` resolves to 23:59:59 and a lead from that rings at a quarter to midnight, which is not a reminder about Friday — inventing a time of day nobody gave is what this project refuses everywhere else, so the limit is recorded instead.
- 14 Aug 2026 — An alarm draws the mobile sentence and three actions — a notification is the smallest screen there is — Push and Snooze are kept apart because one moves the task and the other moves the telling, and Snooze carries its own number because its effect cannot otherwise be seen.
- 14 Aug 2026 — Two timed tasks clash when their windows overlap, and the window is `due_at` plus a per-verb duration default — clashing on stated times alone was the alternative and would have used only facts, where this uses a guess nobody made — the warning names the other task and its time and never the overlap, because duration is a quiet field and the sentence may not state what it was computed from.
- 14 Aug 2026 — Only a `point` anchor occupies a slot — an `end` anchor is 23:59:59, a deadline rather than a booking, and reading one as occupied would make every task due today clash with every other.
- 14 Aug 2026 — The three `alarm_*` fields move out of "always empty in Part A" and into the advanced panel — Part A records what was asked for and a browser cannot wake itself, so the scheduler that makes one sound stays in Part B along with the push it would need.
- 14 Aug 2026 — `capacity_min_per_day` and `duplicate.threshold` leave config for an account row — three hours is what a full day feels like to one person, and a second account inherits a number that was never about them — config holds the value an account starts from, so an account with no row still works.
- 14 Aug 2026 — A repeat spawns its next occurrence when the current one closes, and only then — rolling one record forward would have shown a weekly task done thirty times as zero times in Done, and `push_count` and `first_due_at` would have accumulated across occurrences until the drift they measure meant nothing — spawning on close rather than on schedule means three weeks late is one row, not three.
- 14 Aug 2026 — The next date counts from the schedule and never from when the task was done — rent due the 1st and paid the 4th is next due the 1st, because the anchor is the thing a scheduled repeat is about — stepping from `now` would move that anchor every time it was done late.
- 14 Aug 2026 — `spawned_from` records which completion produced an occurrence — Undone has to take back what Done created, and without it the press leaves the row that came back beside the row that was made.
- 14 Aug 2026 — The type chip offers three and hides eleven behind an advanced panel — `verb_to_type` maps a verb to one type, so there are no runners-up and alternates would have to be guessed from a table with nothing behind it — a fixed short list is honest where a guessed one is not.
- 14 Aug 2026 — A small screen draws `card_reason_short`: no trailing clause, and an overdue lead collapsed to one word — the clauses explain a position the position already shows, and `Overdue since Wednesday` names a day that stopped helping the moment it passed — the hedge survives, because `around` changes what the date means rather than decorating it.
- 14 Aug 2026 — `Park` leaves and `Pick time` takes its place beside `Pick date` — `Park` had sat in config since Stage 2 with no rule behind it, and a button that does nothing is worse than one that is missing — both pickers type what they pick, so a date still arrives one way.
- 14 Aug 2026 — The load is never drawn and instead chooses the push targets: a day over capacity is not offered while a lighter one further out is — the version that annotated each button with what the day held was the one he did not want, and a control that silently avoids a full day does the same work with nothing to read — when every day ahead is full they are all offered, because a press with nowhere to land is worse.
- 14 Aug 2026 — Duration leaves the screen entirely and the badge with it — `est_duration_min` orders Ideas, ranks sixth and measures a day's load, all of which the engine does and none of which the reader needs to see, and a verb without minutes was one word explaining nothing — `result_row` keeps `due_phrase_short` alone.
- 14 Aug 2026 — Three tabs and a toggle: `Tasks` `Ideas` `Done`, with `Today` `Tomorrow` `Upcoming` inside the first — Part A had no tab bar since Stage 1 because a tab nothing fills is a value with no origin, and `deadline_band` fills these, which is what makes the reversal clean rather than a change of mind — overdue sits in Today, because a separate place for it means the tab opened first is not the real day.
- 14 Aug 2026 — Done tasks are reachable again, through a tab, with `undone` to bring one back — the store session put them out of reach of everything on purpose, and a tapped-by-mistake row had no way home — a Done row is a title alone, since `Overdue since Friday` on a finished task describes a deadline that no longer applies.
- 14 Aug 2026 — A push sets `due_at` and touches no words — once a task is added its typed line is gone from the screen and only `title` is drawn, so there is nothing in view for a push to contradict, and `raw_text` stays as provenance nobody reads — the second way a date can arrive, refused twice before, does not arise.
- 14 Aug 2026 — An edit that finds no date in the line leaves the date alone rather than clearing it — a title carries no date words by construction, so clearing on that evidence would destroy the date on every edit of every task — the date chip is what shows it and the one control that clears it.
- 14 Aug 2026 — Push targets come from `date_precision` and carry the load on the day they land on — pushing into a fuller day is the mistake the control exists to prevent, and a target at a precision nobody gave claims one they did not — an overdue task pushes to today, because a task already late is not helped by being later.
- 14 Aug 2026 — `push_count` and `first_due_at` enter the record — everything else in a `Task` is a snapshot and nothing knew how often a task had already moved, which is the one thing that separates a busy week from a task that is wrong — significance is not weighted into the load, because a High task does not take longer and already decides who moves as factor 2.
- 14 Aug 2026 — An empty list screen shows nothing at all — the toggle and the `+` are already on it and they say what to do, and a sentence explaining an empty list is the app talking about itself — which closes the last MVP decision and matches the empty search, reached from the other end.
- 14 Aug 2026 — `date_spans` records where the date expression sits in `raw_text` — `date_phrase` said what was read and never where, so a chip could show `✓ this morning` and the screen had no way to take those words back out of the box — offsets are walked rather than summed, because internal spacing survives into `title`.
- 14 Aug 2026 — A search matching nothing draws nothing at all — `(none)` was written to stop the panel changing shape under the cursor, which was a concern while the box and the list shared a screen, and the MVP puts capture on its own — two headers over no rows is a panel saying no twice.
- 14 Aug 2026 — Capture and edit are one screen and the list is another, reached by `+`; tapping a task on the list opens edit — binding was restricted to search results so nobody could edit a task while believing they were capturing, and a separate capture screen answers that directly.
- 14 Aug 2026 — The screen toggles between Default and Ideas, and both lists come back on every call — drawing Default alone created a dateless task and then hid it, and ten of the twelve real-backlog lines in the key carry no date — an input choosing the list would have made the engine answer a question about the screen.
- 14 Aug 2026 — Ideas is sorted by duration rather than ranked — every factor above `est_duration_min` reads a date, so on a dateless list the first six tie on every row and the order would fall out of the tie-break — which is why the header offers Duration as a choice.
- 14 Aug 2026 — Editing re-resolves the line and carries four things over from the old record: `id`, `created_at`, `pinned` and its state — everything the words decide is re-read, because the words are what changed — saving leaves the bound state.
- 14 Aug 2026 — A four-digit year after a calendar date belongs to the expression, and a stated year is taken as stated, past or future — `20 Aug 2027` resolved to this year and stranded `2027` in the title — `Pick date` writes a year exactly when the picked date is not in the current one, which is the shape this reads.
- 14 Aug 2026 — Live search, four tiers, on `normalised` rather than on the raw line — the string the duplicate rule already starts from, so one line cannot match on a word the other ignores, and a date typed into the box is not a thing to look for — the fuzzy tier keeps its own looser threshold because a result is not a question.
- 14 Aug 2026 — Only open tasks are searched — a done or archived task was put out of the way deliberately and searching it back into view undoes the reason — `DONE` keeps its place in the header vocabulary and draws no rows, and a group with no rows says `(none)` rather than vanishing under the cursor.
- 14 Aug 2026 — `group_header` and `result_row` become one `results` list, an entry per group — two placeholder fields, one a string and one an array, could not hold three groups between them, which is what a placeholder hides until something real is put in it — both names live on as the fields of an entry.
- 14 Aug 2026 — The ranking is built, all nine factors in three tiers — the direction table had sat in the example since Stage 1 where no program could read it, and two of the orderings it needs, `precision_order` and `firmness_order`, were in no config object at all — which is the answer to why a thing described this fully stayed unbuilt: it was written where only a person could reach it.
- 14 Aug 2026 — A trailing clause on `card_reason` speaks only when it is true of the task — `decided_by` names the term that separated a row from its neighbour and three of the eleven have a sentence, so a row separated by `significance` at the default would have read `you marked it Normal`, a claim nobody made — the other terms decide the order silently.
- 14 Aug 2026 — The Default list is built from stored tasks and the four hand-written cards are deleted — `resolve()` returned the same four cards for every line since Stage 3 and the answer key names none of `cards`, `rank_key`, `decided_by`, `group_header` or `result_row`, so nothing ever asked whether they were real — the order is creation order and openly a placeholder until the ranking lands.
- 14 Aug 2026 — Storage is four async calls behind `shell/store.js`, one row per record — Supabase is Postgres and a JSON blob would have been fewer lines now and a rewrite later; async now because a synchronous seam would mean changing every call site rather than four bodies — the undo entry is a row in the same seam, so a real delete survives a refresh.
- 14 Aug 2026 — The panel heading names the list being drawn rather than the typed task's destination — `list_header` answers where the new task goes, and drawing it as the heading made it read `Ideas` above the Default list — nothing could see it while the cards were constants.
- 14 Aug 2026 — Section 6's capture row was a third layout, and the bound-task box had its rails one column right of its own sides and one column narrow — T14 was closed on two panels and a third was never looked at, and every row was 68 wide so nothing but an eye could have seen the box — `gate2.py` now checks that every box in the example closes under the corner it opened at.
- 14 Aug 2026 — `shell/render.js` states a `SHELL_VERSION` and `gate2.py` reads it into VERSIONS — Gate 3's signature named a shell number no file stated, so it was the one signature whose staleness could not be read and the renderer could drift under a signed gate forever — it sits in `render.js` because the panel is what the hand checks at Gate 3.
- 14 Aug 2026 — `card_title` and `significance_row` Example cells corrected against the rules stated beside them — the first kept a marker word the title rule drops, the second emphasised nothing where the rule says the current button is emphasised — found by reading; `gate2.py` checks those tables for shape and count and never for obedience.

- 14 Aug 2026 — Screen 2 is capture and edit on one screen, with the box at the top and the matches below — the alternative was a capture screen and an edit screen, which would have needed two answers to every question the box asks and two places for the date chip to be wrong — the cost is one file large enough that the chip arithmetic and the advanced panel had to leave it to stay under the cap.
- 14 Aug 2026 — The duplicate warning and the clash warning are one dialog carrying one or two sentences — `MVP.md` counts one dialog, and a line that is both a repeat and a collision would otherwise stop a person twice for one press — the button reads `Add anyway` on a capture, `Save anyway` on an edit and `Push anyway` on a push, because a save labelled `Add anyway` describes the wrong action.
- 14 Aug 2026 — The tick chip on the edit screen reads the stored task's card instead of the line — a title carries no date words, so the chip vanished the moment a task was loaded and the edit screen had nothing showing the date and nothing to clear it with, which `MVP.md` says the chip is both — tapping it on a loaded task drops the stored date rather than removing words, so the save writes a record with none and the task moves to Ideas.
- 14 Aug 2026 — Loading a task for editing restores its tapped type and its significance rather than re-deriving both from the title — the words that implied them were consumed on capture, so re-reading a title finds neither and every save was quietly resetting a tapped `deadline` to a derived `action` and a High back to Normal — found by loading a task and reading what came back, which no gate does.
- 14 Aug 2026 — `nextDue` takes at least one step — the loop ran only while the date was still in the past, so a repeat finished early took no step at all and spawned its next occurrence on the same date as the one just closed, which is two open rows for one commitment — the answer key names no repeat case, so nothing was ever going to catch it.
- 14 Aug 2026 — `AlarmType` is declared once, widened — session 93 inserted the three-value union at the top of `types.ts` instead of replacing the old `"none"` at line 46, leaving two declarations, two `TS2300` errors and a `spec.md` claiming a clean compile — nothing in the five-command run is the compiler, so the machine contract has no check under it and that is recorded rather than fixed.

- 14 Aug 2026 — A band word touching a named day narrows that day's window, decided by adjacency — `tomorrow morning` read as a whole day and the band's word left the title having changed no field, so the app was silently answering a question nobody could see it get wrong — a band on a named day does not roll, because the day was named and a named day in the past stays past.
- 14 Aug 2026 — A spelled-out calendar date takes a band over from the lexicon where the two are touching — the date branch ran only when the lexicon found nothing, so `20 Aug morning` was a task for this morning with `20 Aug` left in its title — a band is the only kind a date may displace, since the band is what the date is being narrowed by.
- 14 Aug 2026 — The card says the band only where the day was named — the first version read the finest true granularity and made a bare `morning` typed at two in the afternoon say `Due tomorrow morning`, which claims a word nobody typed; B25 in the key is what caught it — the discriminator is the phrase with `this`, `on` and `next` taken off.
- 14 Aug 2026 — `gate2.py` reads `spec/example.md`'s `config_version` stamp by name and compares it to `config.ts` — the 4 August deviation expired in `spec.md` on 14 August and never in the gate, so the stamp sat at a.13 through two bumps, exempted by name and hidden inside a table cell the gate skips as illustrative — a price nothing can charge is not a price, and this is the third rule found living somewhere only a person could reach.

- 14 Aug 2026 — The app moves to `index.html` at the root and `shell/mvp.html` is deleted — a phone installs an address and that address should be the app rather than a file three folders in, and keeping both would have been two ways in to one screen — the Stage 3 harness keeps `/shell/`, so `check_render` and `check_loud` are untouched.
- 14 Aug 2026 — The service worker is network first with the cache as fallback, not cache first — cache first is faster and serves a version that is no longer in the repository on a phone that cannot be told so, which is the cache defect of sessions 96 and 98 made permanent — the price is that every cold start waits on the network, and it is the right way round.
- 14 Aug 2026 — The worker holds no pre-cache list and caches what has actually been fetched — a list of module names here would be a second inventory to keep in step with the first, and the modules change as the app is built — the cost is that the first visit must be online.
- 14 Aug 2026 — `gate2.py` runs `tsc --strict` and FAILS when the compiler is missing rather than skipping — `types.ts` declared `AlarmType` twice for five sessions while spec.md claimed a clean compile, because the claim lived in prose and nothing could reach it — a check that says nothing when its tool is absent reads as a tooling problem and hides an untested gate, which has happened four times.
- 17 Aug 2026 — `duration_tap`, `firmness_tap` and `notes_text` are inputs to `resolve()` rather than fields the screen patches on to the record afterwards — whatever a person can set while capturing is an input, so the engine returns one complete record and there is no second place a value can be written from — the alternative was three more patch lines beside the nine the save already carries, and the count of things that can silently disagree grows with each one.
- 17 Aug 2026 — A tapped duration outranks the per-verb default AND a comma list's sum, and writes `duration_source` `selected` — the sum is a multiple of the same guess the default is, and the person read the number the engine offered before replacing it — `duration_source` `summed` becomes unreachable on any line where the box was touched, which is correct and narrows where that value is ever seen.
- 17 Aug 2026 — `firmness_tap` carries an `auto` position that clears it rather than a third state meaning normal — a tap that cannot be untapped makes the marker words unreachable for the rest of that task's life — `normal` and `auto` look identical on screen today, because the words most often say normal anyway.
- 17 Aug 2026 — `notes` is read and never matched: `notes_text` does not enter `normalised`, so it reaches neither search nor duplicate detection — stored records are never rewritten, so notes feeding `normalised` later would leave every task captured before that day unsearchable by its own note for ever, which is a permanent split in one field's meaning — the cost is that a note cannot be found by searching for what is in it, and that is the cheaper half to change later.
- 17 Aug 2026 — `check_render` finds its window from the box to the panel's closing rule instead of counting five lines down from the typed line — the count was exact while the chip row was two lines and became wrong the moment a longer label wrapped it to three, reporting every line of a correct panel as a difference — a chip row is now free to grow without the check having to be told.
- 17 Aug 2026 — Cancel and Archive stay members of `row_action` with no control on any screen, and `MVP.md` says so — a row carries Done, Pin, Delete and its push targets, and adding two more presses to it costs the row the two-line reading the whole screen is built on — an archived or cancelled task is unreachable from the UI until a screen is built that wants them, which is stated rather than left to be discovered.
- 17 Aug 2026 — Two hard deadlines on one calendar day warn, through a SECOND check rather than a widening of the clash check — the clash check reads occupied slots and only a `point` anchor occupies one, so a deadline at 23:59:59 can never appear in it, and forcing it to would make every task due today clash with every other — the two checks now have to be kept in step by hand, and the second one cannot say whether the day will hold the work because the load is a per-verb guess the reader never sees.
- 17 Aug 2026 — The deadline warning fires only where BOTH tasks are `hard` — a normal date is a plan and may be moved, so warning on two of those would fire on an ordinary Tuesday and teach the reader to press through the dialog — two things genuinely promised for one day do not warn if neither was marked hard, and marking one is a tap that a person has to know to make.
- 17 Aug 2026 — All warnings share ONE dialog and a line that trips three is stopped once — `mvp.dialog.js` already took an array, which is why a third warning cost no new interruption — the dialog can now carry three sentences and a person reads all of them or none.
- 17 Aug 2026 — `dayWord` in `clash.js` is a second copy of the day-naming rule that lives inside `readDuePhrase` — pulling the day out of `readDuePhrase` is a change to a function six key sections depend on, and this was a screen sentence needing four words of it — the two must be merged the first time they disagree, and the comment above it is where the third copy gets refused.
- 17 Aug 2026 — Screen 3, the account, with an export and a sign-out — signing in had a screen from session 96 and leaving an account had no control anywhere, so the only way out was clearing browser storage by hand — the four counts it shows are the first numbers the app has ever shown a person, allowed because a count of rows is a fact where `est_duration_min` is a default.
- 17 Aug 2026 — The export writes the records exactly as stored, with no shape of its own — a file in an invented shape needs an importer written against it, and the one thing an export must survive is this project changing its mind — the file carries whatever defects the records carry.
- 17 Aug 2026 — The harness's `need()` guard now names `clash_dialog`, `deadline_dialog` and `card_id` — it named seven of ten capture fields and four of five card fields, so `resolve()` could have stopped returning any of the other four in silence — `check_loud` covers them from here, which is the sixth time a check has turned out to be looking at less than it appeared to.
- 17 Aug 2026 — The R2 design system replaces the cool flat one: warm ground, terracotta accent, Fraunces over Figtree, rounded cards on a soft elevation — his call, reversing the same day's decision to change nothing visual, and the token block at the top of `mvp.css` is the whole of the change — the mono utility face is gone, so every control that was subordinate by being mono is now subordinate by weight, size, tracking and contrast, which is four signals rebuilt out of one face instead of two.
- 17 Aug 2026 — Colour now means THREE states, not one: accent is pressable, `#c0492b` is overdue, `#e1eecc` is synced — the old rule was one colour with three jobs, and a warm palette cannot carry overdue on type weight alone the way a cool one could — a fourth coloured thing would end the rule, so the register says the rule out loud on the account screen rather than leaving it to be inferred.
- 17 Aug 2026 — Done is a circle on the row and the word `Done` left the action row with it — the design's own control for it, and the one thing on a row a thumb finds without reading — two controls for one outcome is how two of them come to disagree, and the Done tab reuses the same circle, filled, as Undone.
- 17 Aug 2026 — The design's `+1h` and `+3d` nudges are drawn as the engine's own push targets instead — a band pushes to a band and a day already over capacity is not offered, so two fixed offsets would be two numbers nothing chose — the row now shows two or three labels of differing width where the design had two of equal width.
- 17 Aug 2026 — `Now` is a grouping of the Today list rather than a fourth list — the ranking already puts overdue first, so the block adds a heading, a count and a wash and no new ordering — a heading is only drawn when something else is drawn beside it.
- 17 Aug 2026 — The sync state moves from a line at the foot of the page to a pill in the header — the foot is the last place a person looks and this is the first thing they need when a task has not reached the other device — the pill is drawn from a remembered word and corrected a frame later, because `sync.status()` is async and the draw is not.
- 17 Aug 2026 — A NOT BUILT register is drawn on the account screen and stated in `MVP.md`, and every drawn-but-dead control gets one treatment — a gap nobody can see is a gap that gets rediscovered, and this project has rediscovered four — `decided` and `later` are kept apart in it, because "we chose not to" and "we have not got to it" are different answers and only the second is worth chasing.
- 17 Aug 2026 — `mvp.css` crossed the 400-line cap applying the design, and screen 3's styles left as `mvp.account.css` — the split is by screen, which is the seam every other split in this shell has used — `mvp.edit.css` imports both, so `index.html` still loads one stylesheet.
- 17 Aug 2026 — The gate's inline stylesheet is retuned to the same values as literals rather than as `var()` — it draws before `mvp.css` is fetched, so a token would be an unstyled screen on the one page a first-time user sees — the two files now hold the same six colours in two places and will drift.
- 17 Aug 2026 — The stylesheet link carries the SHELL_VERSION as a query, in three places, and `gate2.py` fails when any of them disagrees — every module is loaded under a fresh date and the stylesheet link was the one thing in the app with no query, so a whole session's design landed on a browser that went on serving the previous one; that is the cache defect of sessions 96 and 98 for the THIRD time, in the file type nobody had thought to bust — a static file cannot carry a date, so it carries a number a person must bump, which is exactly the kind of number that goes stale and is therefore read by the gate rather than trusted.
- 17 Aug 2026 — The `@import` lines inside `mvp.edit.css` carry the same query — an imported sheet is fetched under its own address, so busting the outer file alone leaves the two inner ones stale one level down, which is the same defect one layer further in and would have read as a partial design — three literals now hold one number, and the gate is what holds them together.
- 17 Aug 2026 — The account screen states the running shell and config versions — a design change arrived and there was no way on screen to tell a build that had not reached the browser from a build that had reached it and looked wrong, which cost a round trip to find out — it is the first version number the app has ever shown a person.
- 17 Aug 2026 — Every screen mount RETURNS A HANDLE and the router unmounts the previous one — a screen's `window` listeners outlived it: emptying `#screen` took the list out of the document and left its closure listening, so the next sync event redrew the list over whatever had replaced it, which is being thrown back to the list in the middle of typing a task — and every navigation added another listener, so the fault got worse the longer a session ran. Proved fixed by mounting, unmounting, firing the event and counting redraws.
- 17 Aug 2026 — The web layout is two panes at 940px: the list, and the capture panel beside it, both live — a phone navigates because it has one screen's worth of room and a laptop does not, and making the wide window navigate anyway was the whole of "both look the same" — the breakpoint is now stated twice, in `mvp.css` and in `mvp.js`, because CSS cannot decide whether a tap navigates or loads a panel, and the two numbers must agree.
- 17 Aug 2026 — In the wide layout a row press loads the panel instead of replacing the screen, and the panel draws no Back button — the list keeps its scroll, its tab and its search, which is what it lost on every edit before, and a Back button whose destination is already on screen is a control that explains nothing.
- 17 Aug 2026 — Three keys in the wide layout only: `n`, `/`, `Escape`, and nothing fires while a field has focus — a window that size is used with two hands, and reaching for a mouse to put a caret in a box is what makes a stretched phone app feel like one — `Escape` is the exception that only works inside a field, because "let go of this" is only ever asked from there.
- 17 Aug 2026 — `mvp.css` crossed the cap again and the wide layout left as `mvp.wide.css`, entirely inside one media query — below the breakpoint the file changes nothing, which is what makes "the phone layout is the default" true rather than claimed — a fourth stylesheet is a fourth `?v=` query, which the gate now checks.
- 17 Aug 2026 — Screen 1's chrome is BUILT ONCE and `paint()` changes text and state rather than identity — `draw()` emptied `root` and rebuilt the search input on every keystroke, so the element being typed into was destroyed after the first letter and the `focus()` call that followed addressed a detached node; one letter, then the caret gone — this is the rule screen 2's capture box has had since session 99 and the second screen never got it, which is the fifth time a rule written for one place turned out to be needed in two.
- 17 Aug 2026 — The `Today / Tomorrow / Upcoming` toggle is built once and hidden on the other tabs rather than built conditionally — a control that appears and disappears moves the row above it, and the tab row is what a person is aiming at when they change tabs — the markup now holds three buttons that are not always reachable, which a reader of the DOM has to be told about.
- 17 Aug 2026 — The header date is recomputed on every paint rather than at mount — a tab left open past midnight showed yesterday's date over a list that had already rolled over — one string per paint.
- 17 Aug 2026 — A row and a block of rows left `mvp.list.js` as `mvp.row.js`, with everything they need handed in — the file crossed the 400-line cap making its chrome build-once — a row can no longer read the screen's state behind its own back, which is the shape that let `drawRow` quietly depend on `tab` in the first place.
- 17 Aug 2026 — The stylesheet states its own version in `--css-version` and the wide sheet sets `--wide`, and the app READS BOTH and says so on screen when either disagrees with `SHELL_VERSION` — three sessions were spent working out whether a change had reached the browser or reached it and looked wrong, and a wide window missing its layout sheet looks exactly like a phone, which is the worst kind of failure because it looks like a decision — the check draws nothing when everything agrees, and it is now the loudest thing on the screen when it does not.
- 17 Aug 2026 — The running build is in the HEADER, not only on the account screen — one press away is one press too far when the question is whether a push arrived at all — every screenshot of the app now carries its own version, which is what the last three rounds of this were missing.
- 17 Aug 2026 — A delivery rule, written down because breaking it cost a session: the `changed`-only zip is CUMULATIVE from the last build he has confirmed running, never per-session — session 107's zip held 106's and 107's files and not 105's, so `mvp.account.js` never reached him and the version block he asked for was missing from a build that otherwise looked correct — a half-applied set of files across three sessions is indistinguishable from a bug in the code.
- 17 Aug 2026 — The toast and the truth strip left `mvp.css` as `mvp.chrome.css` — the file crossed the 400-line cap again and both of these belong to no single screen: one is fixed to the window and the other is about the build — fifth stylesheet, fifth `?v=` query, all checked by the gate.
- 17 Aug 2026 — The date chip row is BUILT ONCE, which is the third place this rule has been needed and the second time it arrived late — a picker that is rebuilt while it is open closes without returning anything, and opening a calendar takes several clicks and a few seconds, during which any keystroke or any sync from the other device destroyed the input the calendar was attached to — a date picker that never works and a time picker that works when you are quick is exactly what that produces, and only the tick chip actually changes.
- 17 Aug 2026 — A picker is a button that calls `showPicker()`, not an invisible input stretched over a chip — the old shape relied on the press landing on the browser's own calendar indicator, which sits at the right edge of the field and was under a chip of a different width — the field now takes no clicks at all and stays 1px at the bottom-left of the chip, because the browser anchors the popup to where the field is rather than to what opened it. Where `showPicker` is missing or refused, focus-and-click is the fallback rather than the plan.
- 17 Aug 2026 — `gate2.py` holds `--css-version` in `mvp.css` to `SHELL_VERSION` — the app reads that token to decide whether to accuse the browser of serving an old stylesheet, so a token left behind by a bump would have the app blaming the browser for what the repository got wrong — proved by setting it to 24 and watching the gate name both numbers.
- 17 Aug 2026 — The `Cascade Web` frame is built in full at 1180px — rail, list, detail panel — with everything unbacked DRAWN AND MARKED WIP rather than left out, his instruction — a nav or a panel that grows later moves everything a person has learned the position of, so the shape is put in place now and filled in later — the cost is a screen with eleven controls that do not work, each of which has to say so on a press or it reads as broken.
- 17 Aug 2026 — WIP is ONE treatment everywhere: dimmed, a `WIP` tag, and one sentence on a press naming what is missing and which Part owns it — eleven different ways of saying "not yet" is eleven things to learn — the sentences live in one object per file, so they cannot drift from each other in tone.
- 17 Aug 2026 — THREE things the design draws are deliberately NOT drawn and are NOT marked WIP: a duration on every row, a day's load beside each push target, and a push count — all three are collected and never shown, which is the quiet-fields rule from session 89, so drawing them would reverse a decision rather than fill a gap — `decided` and `not yet` are kept apart here for the same reason they are kept apart in the register.
- 17 Aug 2026 — The detail panel is READ-ONLY and `Edit` hands the task to the capture box — every field on it is already set in the editor, and a second control for one field is how two controls come to disagree, which this project has now paid for four times — the cost is one extra press to change anything from the panel.
- 17 Aug 2026 — TWO breakpoints now: 940 moves the capture box above the list, 1180 adds the rail and the detail panel — a rail needs a first column and a panel needs a third, and at 940 there is room for neither — each number is stated in exactly one stylesheet and in `mvp.js`, and crossing either lands on the list with whatever does not fit closed.


## DELIBERATE DEVIATIONS FROM PROTOCOL

- 14 Aug 2026 — The `config_version` deviation of 4 August expires on its own terms, because the first record has been written to storage — the sealed entry stays exactly as written, since the log is append-only and the gate says so — `spec/example.md` now stamps the live config and every bump from here costs a Stage 1 rewrite and a Gate 1 press, which is the price that entry named.
- 14 Aug 2026 — Storage, which is Stage 7, is built before Stage 6, the screen — the screen is being drawn in a design tool from `MVP.md` rather than in this repository, so waiting for Stage 6 would mean waiting on work that is not happening here — the risk is that the stage checking a drawn screen against the example arrives after the data it draws is already persisted, and five gates are unpressed while it happens.

- 4 Aug 2026 — `spec/example.md` and `shell/resolve.js` may name a `config_version` older than `config.ts` while nothing is deployed — the field exists so a stored record says which config produced it, and no record is stored yet, so tracking it costs a Stage 1 rewrite and a Gate 1 signature for every config bump and buys nothing — the example's stamp is no longer evidence of anything, and the exception expires the first time a record is written to storage at Stage 7.

- 1 Aug 2026 — This session ran several jobs (two decks, a PDF, a spec, then the Stage 1 example) instead of one — the protocol was not in play until partway through — repaid by starting the numbered session log here; from Session 2 onward, one job each.
- 1 Aug 2026 — `CASCADE_PART_A_SPEC.md` exists and spans Stages 2 to 6 while Gate 1 is unpassed — kept as raw material for Stage 2 rather than deleted — the risk is treating it as approved; it is marked stale in FOUND, NOT FIXED and must be rebuilt from the contract, not the other way round.
- 17 Aug 2026 — An alarm is offered ONLY while the line carries a time, and the row disappears with the time — a task due "Friday" resolves to 23:59:59 and a lead off that rings at a quarter to midnight, and the alternative was a second rule inventing a time of day nobody gave — a control that cannot work is worse than an absent one, because its absence reads as a decision and its presence reads as a promise.
- 17 Aug 2026 — `alarm_type` is `none` or `on`; `repeat` and `alarm_repeat_min` are DELETED, not deprecated — every alarm rings two minutes then snoozes itself five, up to five times, so "ring again every" asked for what an alarm already does, and a task that should return another day has `recurrence` — nothing is live, so a field kept for a data set that does not exist would be a field nobody could delete later.
- 17 Aug 2026 — The snooze intervals moved from capture to the ringing alarm: 5 / 10 / 30 / 60, one button each — nobody knows at capture how long they will want, and the number is only wanted with the thing in front of them — 15 was dropped because four buttons is the most a thumb should aim at on a lock screen.
- 17 Aug 2026 — Push is NOT on the lock screen; Done and the four snooze buttons are — choosing a push target reads the day's load off every other task, which the app does and a dead WebView cannot, so moving a due date needs an unlock — his call, and it removed the precomputed-target design and the `PUSH:<iso>` outcome verb with it.
- 17 Aug 2026 — A snooze has TWO HOMES, his call: `alarm_snoozed_until` on the task is the truth and syncs, and the shell keeps its own copy so it can re-ring with the WebView dead — the WebView being dead when an alarm rings is the normal case, not the exception — they are allowed to disagree for as long as an outcome takes to drain.
- 17 Aug 2026 — `alarm_armed_for` is recorded by the shell and is what the diff compares, never the ring time — a snoozed alarm's ring time is not its derived instant, so a diff on ring time either dropped it for being in the past or read it as stale and re-armed it, and opening the app during a snooze ended the snooze — it is also what lets the auto chain run without reporting each step.
- 17 Aug 2026 — An unanswered chain writes `alarm_unanswered_at` and increments `reminder_fatigue`, and `alarm_unanswered` joins `pinned` and `is_hard` as the THIRD tier-1 override — importance is user-set only and must not move, so the escalation needed its own term — third rather than second, so a soft task with a missed alarm cannot jump a hard task without one.
- 17 Aug 2026 — `reminder_fatigue` stops being a working value and becomes a stored column that Part A writes — it was sourced from a Part B structure, so it was zero in every record and read by nothing, and a count that lives only in memory is gone at the next refresh — the cost is two writers with opposite meanings, recorded under FOUND, NOT FIXED as the one thing to re-read when Part B arrives.
- 17 Aug 2026 — The unanswered marker is cleared by a push, a Done, or an edit that MOVES the date, and never by a save that leaves the date alone — his call, and it is the same rule as an edit that finds no date leaving the date alone — `reminder_fatigue` is cleared by none of them, which is the whole point of there being two fields.
- 17 Aug 2026 — Every alarm timing number travels in the `Alarm.set()` payload and none is stated in Kotlin — the interval was written in three places before this session, in two languages, which is the drift pattern this project has paid for five times — changing the ring length or the snooze buttons is a config edit and nothing else.
- 17 Aug 2026 — The auto-snooze steps are NOT reported to the app; only the end of the chain is — five autos would mean five store writes and five sync round trips for one unanswered alarm, and the `armedFor` diff already protects a running chain from an app that opens mid-way — only the chain running out is a fact about the task.
- 17 Aug 2026 — Spent autos do NOT survive a reboot; the chain restarts at five, his call — the alternative was persisting a counter whose whole purpose is to expire — the cost is that a phone rebooting during every chain never reaches the end of one, so the task never escalates, and that failure is silent.
- 17 Aug 2026 — `check_alarm.mjs` is the sixth check, and the first thing in 110 sessions to assert an order out of `rank_key` — no key case names `cards`, `rank_key` or `decided_by`, because the key runs `resolve()` over a typed line and cards are built from `existing_tasks`, so it is a shape mismatch rather than an oversight — a third override reorders every list in the app with the other five checks green, which is the surface every defect found by running the app has been in.
- 17 Aug 2026 — `in 5mins` parses as `in 5 mins`; the number and the unit may be one token or two — a rule written for one spacing had met a person writing another for the third time, after `5.30pm` and `5 pm` — a spacing is not a grammar, and the next rule reading a number beside a word is written for every spacing on the first pass.
- 17 Aug 2026 — The duplicate, clash and deadline comparisons exclude `bound_task_id`, applied once at the call rather than inside the three finders — `resolve()` writes `new_id` into the record on every call including an edit, so the finders' own `t.id !== task.id` guard excluded nothing and every saved edit warned that the task duplicated itself — their guard stays as a second line, and the list, search and Ideas keep reading the whole set.
- 17 Aug 2026 — A saved edit returns to the list on the narrow layout, and the editor header is sticky — an edit is finished when it is saved, and a Back button you have to scroll up to find is not a way out — the wide layout is untouched because its `onBack` is null by design.
- 17 Aug 2026 — The account screen states whether the alarm shell is present and offers the permission request — a browser copy and the Android build look identical on a phone, so a silent alarm had three causes and no evidence — and it is the first caller `requestAlarmPermissions()` has had: session 111 wrote it and `isNativeShell()` and called neither, which nothing in six checks would have found.
- 17 Aug 2026 — The `selftest.py` rule-count fixture reads the number live instead of pinning it — it reported SETUP BROKEN the moment a rule was added, which is the fifth time a fixture pinning a moving value has read as a tooling fault and hidden an untested gate — the config-object fixture two lines above it had already been written the right way.
- 17 Aug 2026 — Going back is the phone's own gesture: the list is the base history entry, every navigation pushes one, and a back from the list closes the app — a drawn Back sits at the top of the screen, which is both out of a thumb's reach on a tall phone and the first thing to scroll away, and making it sticky did not fix either — the drawn button stays and calls `history.back()`, so the two cannot end up one entry apart.
- 17 Aug 2026 — Push targets are back on the alarm, REVERSING his own decision that moving a due date needs an unlock — his instruction — the reason the first design refused them is unchanged, so they are computed when the alarm is ARMED and carried in the payload, and the day's load behind them is as old as the gap between arming and ringing.
- 17 Aug 2026 — An alarm carrying no push targets draws no push row, rather than a disabled button or a default — a button with no target would have to invent a due date, which is refused everywhere else in this project.
- 17 Aug 2026 — Push targets are excluded from the arming diff — a label changes whenever a day fills up, and re-arming every alarm to change two words is a lot of writes for nothing that rings differently.
- 17 Aug 2026 — Done and a push from the alarm bring the app forward and a snooze does not, his call — all three are queued either way, so the unlock makes a change VISIBLE rather than making it happen, and being asked to unlock at six in the morning to acknowledge a snooze is the app taking more than it gave.
- 17 Aug 2026 — A press cannot be applied at unlock with the app closed, and this is refused rather than deferred — it would need Supabase writes from Kotlin: a second copy of the auth token, newest-wins, the offset split and the outbox, in a second language — every duplicated rule in this project has drifted eventually, and this would buy a few seconds of earliness.
- 17 Aug 2026 — Done in the notification shade routes through `AlarmActivity` carrying a `verb` extra, drawing nothing — a broadcast receiver cannot reliably start an activity from the background on Android 10 and later, and an app that comes forward for one Done and not the other is worse than one that never comes forward.
- 17 Aug 2026 — `sw.js` fetches the DOCUMENT with `cache: "reload"` — network-first still went through the browser's own HTTP cache, and GitHub Pages serves `index.html` with a ten-minute lifetime, so for ten minutes after every push the worker was handed the previous page without a request leaving the phone — every other file carries a fresh `?v=` and is a unique URL, so the page is the only one that could go stale and the stylesheet was always behind by exactly one.
- 17 Aug 2026 — `mvp.js` re-points the stylesheet link at SHELL_VERSION when the loaded sheet is behind, instead of only drawing a loud line about it — a loud line about something the reader cannot fix is a complaint, and the app knows the number — what stays loud is the sheet fetched at the right version still reporting the wrong one, which is the repository disagreeing with itself and reads differently now.
- 17 Aug 2026 — The `<meta http-equiv="Cache-Control">` in `index.html` is kept as a LABEL and does nothing — a browser ignores a cache directive inside the document it governs, because by the time the tag is read the response has been stored — it read as a solved problem for four sessions, which is the fourth appearance of the cache defect and the first fix that does not depend on remembering something.
- 17 Aug 2026 — STOPPING THE NOISE NEVER DEPENDS ON AN UNLOCK; every notification action is a broadcast again, Done included — session 114 routed Done through an activity so the app could come forward, and an activity on a locked phone waits for the keyguard, so the line that stops the service did not run until the phone was unlocked and the alarm rang on through a press already made — the pull-forward now happens after the stop, from the receiver, and is allowed to fail, because it is worth having and it is not worth the alarm still ringing.
- 17 Aug 2026 — All four Android permissions are read and requested from the account screen, one row and one button each, plus one that walks through everything missing — they fail differently, so "something is missing" is not something a person can act on, and full-screen access in particular fails silently: the alarm still rings, as a notification, and the lock screen with the four snooze buttons simply never appears — the full-screen settings action is written as a string literal so it compiles against a platform older than the one that added the constant.
- 17 Aug 2026 — The permission list redraws on `visibilitychange` — returning from a system screen is the only moment the answers can have changed and it is exactly the moment the person is looking at the list — the alternative was a stale list telling someone to turn on a switch they had just turned on.
- 17 Aug 2026 — `window.__cascadeBack()` is what the Android shell asks before acting on a back gesture, and `MainActivity` closes the app only when it answers false — `popstate` is enough in a browser, but in a WebView the gesture reaches the activity first and Capacitor's default is to guess from WebView history, where `pushState` entries are same-page and whether they count varies by version — the decision belongs in the file that knows which screen is showing.
- 17 Aug 2026 — A back gesture closes an open dialog before it leaves a screen — a dialog is the nearest thing on the screen and a back aimed at anything is most likely aimed at it — the veil and the Cancel button carry data attributes for no other reason.
- 17 Aug 2026 — `MainActivity.java` is kept in the repository under `android/` rather than described in prose — the plugin registration line was left out of the install instructions once and cost a build that ran, looked right, and had no alarm in it, and a file cannot be half-remembered the way a step can.
- 18 Aug 2026 — `index.html` imports `mvp.js` under `?v=<SHELL_VERSION>` rather than `?v=${Date.now()}`, and `gate2.py` holds the two together the way it holds the stylesheet queries — the clock query made every launch a cold download of the whole graph, and it was only ever protecting against a stale document, which session 115's `cache: "reload"` already prevents — a versioned URL named by a document that is always fresh cannot serve a whole session that never arrived.
- 18 Aug 2026 — `sw.js` serves any URL that states a `?v=` from the cache and skips the network — a versioned address is immutable, since a new version is a new address chosen by a document that is always fetched fresh — the network-first rule stays for everything unversioned, and the header now states the exception beside the rule.
- 18 Aug 2026 — `lemma.js` loads behind the app (`lemmaReady`), not in front of the first paint — it is 3.6 MB, nine tenths of the graph, and the chain reads the model last and lets it only add, so before it arrives a verb resolves exactly as it did the day the model shipped — the stated cost is a lemma-only verb missed in the first second or two, healed by the next keystroke; runners await `lemmaReady` because a check must not race a download.
- 18 Aug 2026 — The Kotlin plugin states its own build (`version()`, build 2), the bridge states the build it was written against, and the account screen draws the gap as a sentence naming the fix — the two halves update at different speeds by design and nothing measured the drift, so a session-113 plugin answered session-117 questions with silence that the screen dressed as `off` — a reading the plugin cannot give now says `unknown`, and a press that cannot work says so where it was pressed.
- 18 Aug 2026 — `requestNotifications` opens the settings screen when Android will no longer show the runtime prompt — a prompt refused twice is never shown again and the request call returns having drawn nothing, which reads as a dead button — whether it can still appear is inferred from `shouldShowRequestPermissionRationale` plus a stored asked-once flag, both false meaning never-asked and both spent meaning settings.
- 18 Aug 2026 — A 60px `+` floats bottom-right on the narrow layout and the wide layouts hide it — the bar's `+` is the most-pressed control in the app and sits where a thumb cannot reach, and at 940px and up the capture box is already on screen so a way into it would be a control with nothing to open — both are the same control and open screen 2 empty.
- 18 Aug 2026 — The page carries `env(safe-area-inset-*)` on all four sides and the sticky editor header offsets its `top` by the same inset — inside the Android shell and the installed web app the page runs edge to edge and the header sat under the status bar — an ordinary browser tab reads the insets as 0px and pays nothing.
- 18 Aug 2026 — The push targets are standard ladders of four or five per precision on a sideways strip that scrolls and draws no scrollbar — two or three fixed rungs meant pushing twice to reach a fortnight, and a column of five would be taller than the row it serves — the rules under them are unchanged: offsets from the task's own date, the load still drops full days, overdue still comes back to today, and the first rung stays in view.
- 18 Aug 2026 — `time_suggestions` (config a.18): five standard times as chips after the pickers, each typing its label into the box — a common time was a dial with several presses, and the date row is a sideways strip now so five chips cost no height — screen vocabulary like `chip_presets`, the engine reads only the words, and the dial stays for every time not on the strip.
- 18 Aug 2026 — The two pickers wear the accent (wash, border, deep ink) — they were grey like the word chips and read as more of the same when they are the way into every date and time the strip does not carry — pressable is what accent already means, so no new colour is spent and the three-state limit holds.
- 18 Aug 2026 — `Workflow` leaves the row — MVP.md has said since session 104 that a row carries Pin, Delete, the Done circle and its push targets, so the fifth control contradicted the written rule, and a dead word on every row is a heavier price than a WIP entry — the rail and the detail panel keep their marked WIP places, so session 109's keep-the-shape decision stands.
- 18 Aug 2026 — A row's sentence is not drawn when it exactly equals `Due <slot>` for the slot it sits under — thirty rows reading `Due today` under a tab called Today was the heading said back, and the sentence exists to add — a time, a hedge, an overdue, a window all differ from the slot's name and keep the sentence, so a task spanning days still states its span; screen decision, engine and key untouched.
- 18 Aug 2026 — Pin and Delete are drawn glyphs with their words kept for screen readers and hovers — the words were the widest thing on the row and said what the shapes say — the pin fills when pinned, so the state is readable without a word.
- 18 Aug 2026 — The `console.log` in `resolve()` is deleted — it ran on every keystroke and wrote the typed line to the console, which is both noise and a line of a person's task in a log nobody asked for — nothing.
- 18 Aug 2026 — **Reverses** two entries from earlier today, quoted as Part 11 requires. The first read: *"The push targets are standard ladders of four or five per precision on a sideways strip that scrolls and draws no scrollbar."* The second read, in part: *"the date row is a sideways strip now so five chips cost no height."* Both superseded on his instruction — *"The scroll should be vertical, not horizontal"* — after build 35 on the phone showed the mechanism: a sideways nowrap strip has the intrinsic width of everything in it, the row's `auto` grid track sizes to intrinsic width with percentage `max-width` ignored, so the track ate the row, the title collapsed and the glyphs stacked; on the capture screen nowrap shrank the picker wrappers until `Pick date` overlapped `Pick time`. The ladders and the time chips themselves stand; only the axis moves.
- 18 Aug 2026 — The push ladder is a column capped at 70px, about two and a half rungs, scrolling vertically with no scrollbar drawn — a column's intrinsic width is one label, which is what the row's grid track was designed around, and the cap holds the row to its old height whatever the ladder's length — the half-cut third rung is the scroll affordance, and the first rung is always in view.
- 18 Aug 2026 — The date chip row wraps as chips do everywhere else and is capped at 96px, two rows and a hint of the third, scrolling vertically — wrap is the one flex arrangement whose intrinsic width is a single chip, so nothing upstream is shoved — the tick chip stays first and visible.
- 18 Aug 2026 — A fixed, paper-coloured, touch-inert strip exactly `env(safe-area-inset-top)` tall sits over the top edge of the page — the session-119 body padding positions only the unscrolled page, since padding scrolls away with the content it wraps, and the account screen mid-scroll slid its text back under the clock — 0px tall in an ordinary browser tab, so it costs nothing there.
- 18 Aug 2026 — The row glyphs state `width` and `height` attributes as well as carrying the stylesheet rule — an SVG with no stated size is 300×150 by default, and a stylesheet arriving late once is all it takes for two of those to shove a row around — the attribute is the floor, the stylesheet stays the source of the number.
- 18 Aug 2026 — The list's search query is lowercased before it reaches `matchTier` — the matcher compares against `normalised`, which is lowercase, and a phone keyboard capitalises the first letter of anything, so `Pcb` scored zero against `pcb pin requirement` and search never worked from a phone — the capture box never hit this because its line is normalised inside the engine, which is why the defect survived every desktop test.
- 18 Aug 2026 — A pick puts no words in the box — his words: *"Selecting a time should not insert any text"* — the box holds what the person typed, the picked words are composed onto the end of the line the engine reads under a `chip_span`, and the tick chip is where the reading shows and where the pick is taken back — a date still arrives one way, through words in the one line, and the engine, the contract's inputs and the key are untouched; proved headlessly before shipping.
- 18 Aug 2026 — A pick on an empty box joins nothing and waits, previewed on the tick chip — composed immediately, the date words themselves became the title (`Tomorrow morning` as a task), which is a capture of no commitment — the pick joins the line the moment there are words to date.
- 18 Aug 2026 — The date block is three pieces: a pinned row (tick, Pick date, Pick time) that never scrolls, a date scroller, and the times row — his call that the way into every unlisted date must always be visible — the two pickers left `chip_presets` (a.19): they are screen furniture, not vocabulary.
- 18 Aug 2026 — Every phrase in `chip_presets` is proved against the engine before it joins, and `day after tomorrow` is out — it reads as `from tomorrow` and strands `day` in the title, so the chip would have corrupted every title it touched — the ten that joined each resolve with a clean title and the expected precision.
- 18 Aug 2026 — Nothing scrollable cuts a chip or a rung in half; the scroller and the push ladder fade at the bottom edge instead — session 120's half-cut affordance read as a defect on the phone, and a fade says `more below` without amputating anything — the caps sit at whole rows now.
- 18 Aug 2026 — The header date belongs to the slot: Today wears today, Tomorrow wears tomorrow, Upcoming says where it starts, Ideas and Done wear nothing — a tab called Tomorrow wearing today's date read as a bug, and was one — the date line is recomputed each paint as before.
- 18 Aug 2026 — **Amends** session 89's quiet rule, quoted as Part 11 requires: *"A duration on every row, a day's load beside each push target, and a push count. All three are collected and none is shown."* On his request each slot now wears its total (`Today · 1h 40m`), the verb's guess where nobody chose one — the ONE place a duration appears, because it is the number that chooses which day to open — rows, push targets and the push count stay quiet exactly as decided.
- 18 Aug 2026 — The advanced panel runs notes, alarm, repeat, firmness, duration, type — his order, the order a person thinks about a task they are refining — type is last because the engine already guessed it and the chips above the panel already offer the likely three.
- 18 Aug 2026 — The Back button left screen 2 and the editor's head exists only while a task is bound — the Android gesture asks `__cascadeBack()`, the browser has its own Back, and a save returns on its own, so the word was renting the top of the screen for a fourth way out — the bound-title chip and its ✕ keep the head while editing.
- 18 Aug 2026 — The account screen draws a Turn on button for `unknown` as well as `off` — a switch the old shell cannot read may still be off, and a person staring at `unknown` with nothing to press is stuck — pressing it on a too-old shell answers with the rebuild sentence beside the button.
- 18 Aug 2026 — Export inside the Android shell is recorded as broken rather than quietly worked around — the WebView has no blob-download handler, so the fix is a share sheet or a Kotlin write, and both belong to the next Kotlin session — in a browser it lands in Downloads as `cascade-<date>.json`, which answers where it stores.
- 18 Aug 2026 — The chip scrollers show one row and draw their scrollbar — build 37 capped at two rows, hid the scrollbar and put the fade in the row gap, so nothing said `more below` and the scroll read as vanished, which it functionally was — a thin scrollbar, a fade over real chips, and `overscroll-behavior: contain` so the strip scrolls instead of the page.
- 18 Aug 2026 — **Reverses** session 91, quoted as Part 11 requires: *"The type chip offers three and hides eleven behind an advanced panel — `verb_to_type` maps a verb to one type, so there are no runners-up and alternates would have to be guessed from a table with nothing behind it — a fixed short list is honest where a guessed one is not."* Requested directly this session — the three chips could not answer "how many types are there", which is what he asked the screen — the control is a native dropdown of all fourteen with the engine's guess as its value, which is what the example has drawn since Stage 1 (`⟨action ▾⟩`), so the chips were the deviation. Everything the old entry protected stands: one derived type, no runners-up, nothing guessed. `type_suggestions` stays in config; the Stage 3 harness still reads it.
- 18 Aug 2026 — The Type group left the advanced panel — the dropdown beside the ⋯ holds all fourteen, so a second copy in the panel was the same control drawn twice — the panel runs notes, alarm, repeat, firmness, duration.
- 18 Aug 2026 — The alarm's missing-time note is four words, `Needs an exact time.` — the old three sentences explained midnight arithmetic to a person who only needed to know what was missing — his edit, near verbatim.
- 18 Aug 2026 — Every lock-screen alarm outcome wrote nothing: `apply()` passed the record alone where the store's contract is `update(id, record)`, the throw landed in a catch, and Done, Push, Snooze and the unanswered escalation were all silently lost — all four branches now pass the id — `check_alarm.mjs` proves the pure functions and never calls `apply()`, so this walked past it, recorded rather than hidden.
- 18 Aug 2026 — A lock-screen Done spawns the repeat's next occurrence exactly as the list's Done does — before this only the in-app press spawned, so closing a weekly task from the alarm screen silently ended its series.
- 18 Aug 2026 — The alarm toggle lives on the capture screen while the line carries an exact time, stating the instant it will ring (`rings 2:45pm`, due minus lead) — setting a ring should not require opening the panel, and a toggle that hides its consequence is a guess — no time, no row; the panel's four words cover that case.
- 18 Aug 2026 — The date chips are two side-by-side columns, each its own ~3-chip vertical scroll with drawn scrollbar and fade — near phrases (`This …`, `Tonight`) left, later phrases right — one wrapped strip made today and next month neighbours, and the first glance should answer `now or later`.
- 18 Aug 2026 — `year` joined the repeat units — one line in `step()`, `n * 12` months, so 29 Feb rolls to 1 Mar by the arithmetic months already rely on — and the panel states the app's reading of a set repeat as one sentence, `every week on Tuesday at 3pm`, derived from the due date the schedule anchors on, because a control that echoes its meaning back cannot be silently misread.
- 18 Aug 2026 — Screen dressing from his slides: number inputs wear a sheet ground and a drawn edge (a field, not one more chip); the sticky header wears the paper so no band sits behind the bound chip; the avatar is three dots; the narrow bar's `+` is gone (the FAB is the same control where a thumb is; wide keeps the `+`); the push ladder shows three rungs before the fade.
- 18 Aug 2026 — With text in the search box the pool is every band plus Ideas rather than the open slot — scoped, a task in Upcoming answered `not in Today`, which reads as `does not exist` — the placeholder says `search all tasks`, Done stays out unless the Done tab is open, and a slot or tab press clears the box because a navigation is an answer accepted.
- 18 Aug 2026 — **Amends** session 121's "picks put no words in the box", quoted: *"his `selecting a time should not insert any text` — picks no longer appear in the box; picked date/time words compose onto the end of the one line the engine reads"* — a typed time outranked every later pick by sitting earlier in the composed line, so on his direct rule ("whatever is typed or click last") a time pick now replaces the typed time token in the line, and typing a time takes a standing pick back — replacing a time the person is superseding is not inserting words they did not choose — the detection regex covers the usual typed shapes, and a form it misses keeps typed-wins, the safe side.
- 18 Aug 2026 — A press on the push ladder that travelled more than 8px is a scroll and is swallowed before any rung hears it — dragging the capped ladder ended on a rung and the browser fired its click, so scrolling pushed the task.
- 18 Aug 2026 — A `Today` rung leads the ladder for time-, band- and day-precision tasks sitting on a later day — the ladders only pushed outward, and a tomorrow that frees up could not be pulled in — it lands today at the task's own clock time, or an hour from now when that instant has gone, because a pull-forward that arrives overdue is a trap.
- 18 Aug 2026 — The capture-screen alarm sits directly under the box and states the day as well as the clock — `rings 4:45pm today` / `tomorrow` / `on Monday` inside six days / `on 20th August` beyond — plus `· repeats` when the task does, his wording near verbatim.
- 18 Aug 2026 — The repeat group's Never button lives beside the label and wears the app's sentence once a repeat is set, tapping it always the way back to never; the number and four units fit one line; the panel's number inputs fire on `change` rather than `input` — repainting per digit rebuilt the field out from under the caret, which is what "editing shifts focus with every typed number" was.
- 18 Aug 2026 — The screen says `Later` where the engine says `Upcoming` — the band name in every record and rule is unchanged — and no slot wears the pressed mark while a search is on, since with text in the box the pool is everything not done.
- 20 Aug 2026 — The press-vs-scroll rule left `mvp.row.js` for `mvp.tap.js` and every scrolling strip wears it — 8px on either axis, and a touch held past 600ms — the session-124 guard was ladder-only, touch-only, Y-only and unbounded in time, and the date columns and times row had none at all — 600ms is a chosen number and not a measured one.
- 20 Aug 2026 — The search prefix tier matches the start of ANY WORD, not only the start of the title, scored 0.9 of a whole-title prefix — `Plant` found nothing while `Plants` found two, because `water plants` does not begin with `plant` and the fuzzy tier scores that pair 0.42 against a 0.5 threshold — a query is now a prefix question about words rather than about titles, which is looser than the duplicate rule and deliberately so.
- 20 Aug 2026 — `check_search.mjs` is the seventh check — no key case can name `results`, because the key runs `resolve()` over a typed line and a search tier compares that line to stored tasks — the largest untested surface is one file smaller.
- 20 Aug 2026 — The lead is a 0–60 slider beside the Alarm toggle and LEFT the advanced panel, his ask — one field with two controls is how two controls come to disagree — a stored lead above an hour still rings and can no longer be set from any screen.
- 20 Aug 2026 — The duration is a slider over a ladder of real durations rather than a linear range, his ask, replacing seven controls with one — a linear slider across `duration_max`'s 182 days cannot land on twenty minutes — the ladder's rungs are a choice and the values between them are unreachable.
- 20 Aug 2026 — An add returns to the list on the narrow layout and its toast travels with it, his words — a save has returned since session 112 and the twenty-times-a-day press had not — the Undo offer moves screens with the message rather than dying with the screen that drew it.
- 20 Aug 2026 — Screen 4 lists every armed alarm, reached by a button after `Done`, his ask — a row there answers "when will this ring" where a tab answers "when is this owed", and the set is exactly `desiredAlarms()`'s so the screen cannot hold a second opinion — `Alarm off` on a repeating task ends the ring for the whole series, because `spawn()` inherits `alarm_type`, and the row says so.
- 20 Aug 2026 — `nextDue()` anchors on `first_due_at || due_at` — it stepped from `due_at`, which a push overwrites, so a pushed occurrence moved the whole series, the drift `repeat.js` says it prevents — a saved edit that moves the date clears `first_due_at`, because a restated date is a new start and a push is a temporary move away from one.
- 20 Aug 2026 — Everything that ends or moves an alarm calls `alarmCleared()` — it was exported and called by nothing while its rule was hand-copied into three files, and the two places that missed it were the in-app Done and the saved edit, so a completion kept a live snooze and an edited date rang at the old time — one function, and `check_alarm.mjs` now names it.
- 20 Aug 2026 — The Android lock screen wears paper, ink and signal instead of ink-on-dark, his words — the one surface a person meets half asleep was the only one that did not look like the app — it needs an APK rebuild, and the palette is now duplicated in two languages rather than one.
- 20 Aug 2026 — An overdue repeat is left where it is and the alarms screen says `will not ring again until the date moves` — a past instant is never armed and the next occurrence only exists after a Done, so one unanswered chain ends a series quietly — moving a date the app invented is refused, and the real fix is a rule about repeats that is his to make.
- 20 Aug 2026 — An occurrence the schedule has already overtaken is closed as `cancelled` on app open and the next scheduled date is spawned, his call from three options — a repeat only advances on a Done, and a past instant is never armed, so one unanswered chain ended a series in silence — `cancelled` rather than `done` because it was not done, and the row stays on the Done tab so the miss is visible; the test is one whole interval, not one minute, so a task a person still means to clear is never moved.
- 20 Aug 2026 — The catch-up takes no undo slot and derives the new occurrence's id from the closed one and its date — the single undo entry belongs to the last thing a person did, and two devices opening at once would otherwise add two rows — so this one write cannot be undone by pressing Undo, and the cancelled row is the way back.
- 20 Aug 2026 — `mvp.js` split at the 400-line cap: the stylesheet repair and the loud line left as `mvp.truth.js`, handed the root and the breakpoint — a router does not need to know how a stylesheet states its version — the four loud states are unchanged and still reachable only by hand.
- 20 Aug 2026 — A repeat's alarm follows its RULE rather than its open occurrence: `nextRing()` steps the derived instant forward until it is ahead of the clock — a spent instant is never armed and the next occurrence only exists after a Done, so a daily alarm rang once and never again while the screen still said `every day` — the record is untouched and still reads overdue, which is true; only the ring moves, and only forward.
- 20 Aug 2026 — `armedFor` in the payload is the schedule's instant, not `alarmAt()` — it is the diff key, so a daily alarm would have looked identical on two days and never re-armed — it still tells a snoozed alarm from a stale one, which is what it was added for.
- 20 Aug 2026 — The alarms screen speaks in three shapes and no clauses: rings, snoozed until, missed — `will not ring again until the date moves` was his slide's example of hard to understand, and for a repeat it is no longer true.
- 20 Aug 2026 — The editor speaks about the date the SAVE WILL KEEP rather than the date in the box — the box holds `title`, whose date words are gone, so editing a task with an alarm showed no alarm and told him it needed an exact time — one rule, and `keepDate` already wrote it.
- 20 Aug 2026 — The Alarm group left the advanced panel, his call — the toggle has been under the box since session 123 and this was the same field with a second control — everything about an alarm is on one row now, including why there is no toggle.
- 20 Aug 2026 — The push ladder left the alarms screen, his call — it was the only control there that moved the date rather than the ring, and the title opens the editor — rungs stay on list rows and the lock screen, and the acts wear pills so they read as buttons.
- 20 Aug 2026 — A Done row states the day it was finished and carries a `Revive` button, both his — `closed_at` is a fact rather than a guess so the quiet rule stands, and the filled circle has meant Undone since session 104 with nothing on screen saying so.
- 20 Aug 2026 — The lock screen draws the alarm shell's build, stated once as `CascadeAlarmPlugin.SHELL_BUILD` and read by `version()` too — a number in two places would be the account screen and the alarm disagreeing about which shell is running — raised to 3 with `ALARM_SHELL_EXPECTED`, so an un-rebuilt APK asks for the rebuild instead of just looking wrong.
- 20 Aug 2026 — `apply()` left `alarm.bridge.js` as `alarm.apply.js` and takes the store as an argument, and `catchUpRepeats()` stopped importing one — a write path that imports the real store cannot be imported by any check, which is why session 123's four lost outcomes ran green for four days — the seam is the fix, not the assertions on top of it.
- 20 Aug 2026 — `check_writes.mjs` is the eighth check and it asserts the SHAPE OF THE CALL, not only the value: its store throws when `update` is handed anything but `(id, record)` — every value session 123 computed was correct and the defect was in the call — proved by reverting one branch and watching the run fail.
- 20 Aug 2026 — `SHELL_BUILD` moved into the ONE companion object `CascadeAlarmPlugin` already had — session 126 added a second one and shipped it, and Kotlin allows exactly one per class — it failed on his machine and nowhere else, because nothing in this project compiles Kotlin: `gate2.py` runs `tsc --strict` over the web half and the six Kotlin files have no equivalent, which is now a written gap rather than one found twice.
- 20 Aug 2026 — `CANCEL` closes one occurrence as cancelled and hands a repeat its next one, his ask — cancelling tonight's run is not cancelling the habit, and `cancelled` rather than `done` because it was not done — the row stays on the Done tab so the miss is countable.
- 20 Aug 2026 — `DISMISS` ends the ring and touches nothing else — `alarm_type` would end the series and the date would move a commitment to silence a noise — a repeat arms its next occurrence on the following sync, which only works because a repeat's ring already follows its rule.
- 20 Aug 2026 — The `time` push ladder holds +1, +2, +3 and +4 hours, his ask on the lock screen — an exact time is the precision where an hour is a real answer and the middle two needed two presses — widened in `push.js`, so the list rows gained them too rather than the lock screen keeping a private ladder.
- 20 Aug 2026 — The lock screen carries five push targets in a sideways scroller and a `Pick…` button opening date then time — two rungs could not reach tomorrow, and five equal slices of a phone's width are five unreadable buttons — the picker composes local wall clock with the offset read off the calendar, the one place this shell writes a date.
- 20 Aug 2026 — `gate2.py` fails any relative import in `shell/` that does not carry `?v=` — session 126 wrote one plain import in `alarm.js` and it survived three builds, and a cached module missing an export takes down every module above it — `resolve.js` and `resolve.stage3.js` are a stated warning because the node checks import them with no version to pass.
- 20 Aug 2026 — The alarms screen reads the shell's own `list()` and marks a row `NOT armed on this phone` — a silent phone could be a permission, an old APK or the arming pass, and every screen until now drew only what the app intended — the app's intention and the phone's state are two different facts and the screen shows both.
- 20 Aug 2026 — `popstate` restores a screen with `push: false` — `showAlarms()` pushed a fresh entry when called from a back gesture, so back put screen 4 on top of itself and became a loop with no way out but killing the app — restoring a screen is not navigating to it.
- 20 Aug 2026 — Reviving a repeat lands it on the next date its own rule gives and clears `first_due_at` — the catch-up runs on every open and cancelled the stale occurrence straight back again, so the press worked and was undone before he saw it — a one-off is reopened where it was, overdue and visibly so.
- 20 Aug 2026 — The editor keeps the record it was handed instead of finding it in `all` — `all` is filled by `reload()`, which lands after `bind()`, so the alarm row's fallback to the stored date found nothing on the first paint and nothing repaints an untouched screen — session 126 fixed the rule and not the timing.
- 20 Aug 2026 — A cancelled row says `Cancelled`, not `Done` — the tab has held both states since the catch-up was built — a task the calendar walked past was reading as an achievement.
- 20 Aug 2026 — Screen 4 has a second view listing everything that repeats, alarm or not, his ask — the two lists overlap and neither contains the other, so a repeat with no alarm had nowhere to be removed from.
- 20 Aug 2026 — The account screen links the APK at freezigo-raj.github.io/cascade/app-debug.apk, his ask — the sentence asking for a rebuild was only useful to the person who could build one — an anchor rather than a button, so it copies and long-presses like an address.
- 20 Aug 2026 — `alarm.apply.js` reads a stored instant WITH its offset — it dropped one, so every lock-screen Done stamped `updated_at` five and a half hours into the future and the database's newest-wins trigger silently refused every later write to that task, including a Revive — the bridge and the alarms screen always read the offset; this was the one place whose number is compared by Postgres.
- 20 Aug 2026 — `check_writes.mjs` asserts the DRIFT of a Done stamp at a positive and a negative offset, against a task written seconds ago — the stamp is `max(stored + 1s, now)`, so a stale fixture hides the fault behind `now` and the first version of the assertion passed against the broken line.
- 20 Aug 2026 — The bin cancels rather than erases, his ask — a task that leaves no trace is one nobody can answer a question about later — it writes the same `cancelled` state the catch-up and the lock screen write, so the Done tab holds one kind of closed row.
- 20 Aug 2026 — `Delete for good` on a Done row is the only control that erases a task — without one the store can never be cleaned — it is reachable only from a row that is already closed, so erasing costs two decisions instead of one.
- 20 Aug 2026 — The alarm row falls back to the stored date with `||` and not `??` — `resolve()` returns `due_at: ""` for a dateless line, an empty string that `??` passes straight through — sessions 126 and 129 each fixed a real half of this defect and both read past the operator.
- 20 Aug 2026 — The lock screen carries four rungs and two pickers on a line of their own, his slides — `Pick…` inside the scroller sat behind four approximations of the exact answer, and moving a task to another day is a decision worth being awake for — `Pick time` is one dialog and sends a time already gone to tomorrow.
- 20 Aug 2026 — The APK link is `target="_blank"` and carries `data-perm` — `download` is ignored across origins and a WebView navigating to an APK on its own host does nothing at all, and the account's alarm block sweeps `[data-perm]` nodes on every redraw so an unmarked link was duplicated on the way back from a settings screen.
- 20 Aug 2026 — A re-uploaded file with the same name is a NEW file — session 130 answered the two items typed in chat and never opened `UI_review_R2.pptx`, and three of its five items existed only in the deck — every slide's text is dumped before anything is built.
